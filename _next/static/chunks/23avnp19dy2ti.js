(globalThis.TURBOPACK||(globalThis.TURBOPACK=[])).push(["object"==typeof document?document.currentScript:void 0,26033,e=>{"use strict";let t=[{slug:"py-count-closed-won",chapter:1,title:"Count the deals we won",difficulty:"warmup",prompt:'`deals` is a pandas DataFrame with a `stage` column.\n\nSet `result` to the **number** of rows where `stage == "closed_won"`.',starterCode:`# deals is already loaded as a pandas DataFrame.
won = deals[deals["stage"] == "closed_won"]
result = None  # TODO: how many rows are in won?`,solution:`won = deals[deals["stage"] == "closed_won"]
result = len(won)`,dpmConnection:{text:"Boolean filtering is the pandas equivalent of a WHERE clause. Being able to reproduce a number a dashboard shows is how a Data PM verifies it instead of trusting it.",kbSlug:"four-key-shifts"},hint:"len(won) or won.shape[0]."},{slug:"py-deals-closed-value",chapter:1,title:"Functional metric: deals_closed_value",difficulty:"warmup",prompt:"Set `result` to **deals_closed_value**: the sum of `amount` for all `closed_won` deals.",starterCode:`won = deals[deals["stage"] == "closed_won"]
result = None  # TODO: sum of won["amount"]`,solution:`won = deals[deals["stage"] == "closed_won"]
result = won["amount"].sum()`,dpmConnection:{text:"Same functional metric you computed in SQL. A Data PM who can compute a metric two independent ways can catch a pipeline bug that only shows up in one path.",kbSlug:"metric-types"},hint:"won['amount'].sum() — a numpy type is fine; the grader converts it."},{slug:"py-revenue-generated",chapter:1,title:"North Star metric: revenue_generated",difficulty:"warmup",prompt:"Set `result` to **revenue_generated**: the sum of `amount` across the `transactions` DataFrame.",starterCode:'result = None  # TODO: total of transactions["amount"]',solution:'result = transactions["amount"].sum()',dpmConnection:{text:"A different table than deals. The gap between revenue and won-deal value is renewals plus upsells (and, until chapter 3, some duplicates). Explaining that gap to the CFO is chapter 6.",kbSlug:"metric-types"},hint:"transactions['amount'].sum()"},{slug:"py-conversion-rate",chapter:1,title:"Granular metric: conversion_rate",difficulty:"warmup",prompt:"Set `result` to **conversion_rate** = won ÷ (won + lost), where won = `closed_won` deals and lost = `closed_lost` deals. Open deals are excluded. Round to 4 decimals.",starterCode:`won  = (deals["stage"] == "closed_won").sum()
lost = (deals["stage"] == "closed_lost").sum()
result = None  # TODO: round(won / (won + lost), 4)`,solution:`won = (deals["stage"] == "closed_won").sum()
lost = (deals["stage"] == "closed_lost").sum()
result = round(float(won / (won + lost)), 4)`,dpmConnection:{text:"Summing a boolean Series is the pandas idiom for a conditional COUNT, the same trick as CASE WHEN in SQL. The definition decision (exclude open deals) must be written down or every consumer computes it differently.",kbSlug:"ontology-as-infrastructure"},hint:"round(won / (won + lost), 4). Wrap in float() if you get a numpy type."},{slug:"py-groupby-account",chapter:2,title:"Won value by account (merge + groupby)",difficulty:"core",prompt:"Merge `deals` with `accounts` on `account_id`, keep only `closed_won` deals, then compute total won amount per **account_name**.\n\nSet `result` to a **list of dicts** with keys `account_name` and `total_won`, sorted by `total_won` descending, then `account_name` ascending.",starterCode:`won = deals[deals["stage"] == "closed_won"]
merged = won.merge(accounts, on="account_id")
grouped = (
    merged.groupby("account_name", as_index=False)["amount"]
    .sum()
    .rename(columns={"amount": "total_won"})
    .sort_values(["total_won", "account_name"], ascending=[False, True])
)
result = None  # TODO: grouped.to_dict("records")`,solution:`won = deals[deals["stage"] == "closed_won"]
merged = won.merge(accounts, on="account_id")
grouped = (merged.groupby("account_name", as_index=False)["amount"].sum()
    .rename(columns={"amount": "total_won"})
    .sort_values(["total_won", "account_name"], ascending=[False, True]))
result = grouped.to_dict("records")`,orderMatters:!0,dpmConnection:{text:"merge = JOIN, groupby = GROUP BY. This is the logical model's 'Relationships' (accounts 1:N deals) turned into a measure, the exact structure of a Metric Dependency Tree node.",kbSlug:"canvas-data-product-design"},hint:"The starter does the work; finish with .to_dict('records')."},{slug:"py-rep-performance",chapter:2,title:"Top-performing rep",difficulty:"core",prompt:"Group `closed_won` deals by `owner` and sum `amount`. Set `result` to a **dict** with keys `owner` and `total_won` for the single top rep.",starterCode:`won = deals[deals["stage"] == "closed_won"]
by_owner = won.groupby("owner")["amount"].sum().sort_values(ascending=False)
result = None  # TODO: {"owner": ..., "total_won": ...} for the first row`,solution:`won = deals[deals["stage"] == "closed_won"]
by_owner = won.groupby("owner")["amount"].sum().sort_values(ascending=False)
result = {"owner": by_owner.index[0], "total_won": int(by_owner.iloc[0])}`,dpmConnection:{text:"Reading one row out of a grouped Series: by_owner.index[0] and by_owner.iloc[0]. Look at the concentration: if one rep is a big share of won value, one bad record on their deals swings the North Star.",kbSlug:"bullseye-data-product-market-fit"},hint:"result = {'owner': by_owner.index[0], 'total_won': int(by_owner.iloc[0])}"},{slug:"py-region-conversion",chapter:2,title:"conversion_rate by region",difficulty:"core",prompt:"Compute **conversion_rate** per **region** (region is on `accounts`; stage on `deals`): won ÷ (won + lost), open deals excluded, rounded to 4 decimals.\n\nSet `result` to a **list of dicts** with keys `region` and `conversion_rate`, sorted by region ascending.",starterCode:`closed = deals[deals["stage"].isin(["closed_won", "closed_lost"])]
merged = closed.merge(accounts, on="account_id")
merged = merged.assign(is_won=(merged["stage"] == "closed_won").astype(int))
grouped = (
    merged.groupby("region", as_index=False)["is_won"]
    .mean()
    .rename(columns={"is_won": "conversion_rate"})
    .sort_values("region")
)
grouped["conversion_rate"] = grouped["conversion_rate"].round(4)
result = None  # TODO: grouped.to_dict("records")`,solution:`closed = deals[deals["stage"].isin(["closed_won", "closed_lost"])]
merged = closed.merge(accounts, on="account_id")
merged = merged.assign(is_won=(merged["stage"] == "closed_won").astype(int))
grouped = (merged.groupby("region", as_index=False)["is_won"].mean()
    .rename(columns={"is_won": "conversion_rate"}).sort_values("region"))
grouped["conversion_rate"] = grouped["conversion_rate"].round(4)
result = grouped.to_dict("records")`,orderMatters:!0,dpmConnection:{text:"The mean of a 0/1 indicator column IS the rate: a compact pandas idiom worth memorizing. Cross-entity slicing (measure on deals, dimension on accounts) is where join mistakes silently corrupt a metric.",kbSlug:"canvas-data-product-design"},hint:"The starter is complete except the last line."},{slug:"py-rca-source-lost",chapter:2,title:"Where are we losing deals?",difficulty:"core",prompt:"For each deal `source`, compute **lost_value** (sum of `amount` for `closed_lost` deals) and **lost_count**. Set `result` to a **list of dicts** with keys `source`, `lost_value`, `lost_count`, sorted by `lost_value` descending.",starterCode:`lost = deals[deals["stage"] == "closed_lost"]
grouped = (
    lost.groupby("source")
    .agg(lost_value=("amount", "sum"), lost_count=("deal_id", "count"))
    .reset_index()
    .sort_values("lost_value", ascending=False)
)
result = None  # TODO: grouped.to_dict("records")`,solution:`lost = deals[deals["stage"] == "closed_lost"]
grouped = (lost.groupby("source").agg(lost_value=("amount", "sum"), lost_count=("deal_id", "count"))
    .reset_index().sort_values("lost_value", ascending=False))
result = grouped.to_dict("records")`,orderMatters:!0,dpmConnection:{text:"Named aggregation (.agg(name=(col, fn))) computes several measures at once. The output is the raw material for an RCA conversation; the Data PM's job starts after this query.",kbSlug:"proof-of-value-performance"},hint:"grouped.to_dict('records')."}];e.s(["pythonExercises",0,t,"pythonExercisesForChapter",0,function(e){return t.filter(t=>t.chapter===e)}])},8908,e=>{"use strict";let t="CASE stage WHEN 'prospecting' THEN 1 WHEN 'qualified' THEN 2 WHEN 'proposal' THEN 3 WHEN 'negotiation' THEN 4 WHEN 'closed_won' THEN 5 ELSE 6 END",a=[{slug:"sql-select-closed-won",chapter:1,title:"See the rows behind the number",difficulty:"warmup",prompt:"Before you argue about a conversion rate, look at the rows that feed it.\n\nReturn the **deal_id**, **account_id**, and **amount** of every deal whose `stage` is `closed_won`, ordered by `deal_id`.",starterQuery:`SELECT deal_id, account_id, amount
FROM deals
WHERE -- your condition here
ORDER BY deal_id;`,solution:"SELECT deal_id, account_id, amount FROM deals WHERE stage = 'closed_won' ORDER BY deal_id;",orderMatters:!0,dpmConnection:{text:"Filtering to closed_won is the first step of the functional metric deals_closed_value. A Data PM who can point at the exact rows behind an aggregate can defend it; one who can't is trusting the dashboard.",kbSlug:"metric-types"},hint:"Compare the stage column to the string 'closed_won' (single quotes) in the WHERE clause."},{slug:"sql-deals-closed-value",chapter:1,title:"Functional metric: deals_closed_value",difficulty:"warmup",prompt:"Compute **deals_closed_value**: the total `amount` across all deals in stage `closed_won`. Return one row, one column named `deals_closed_value`.",starterQuery:`SELECT -- aggregate here
FROM deals
WHERE stage = 'closed_won';`,solution:"SELECT SUM(amount) AS deals_closed_value FROM deals WHERE stage = 'closed_won';",dpmConnection:{text:"This is the Playbook's functional metric for Sales: bookings. It rolls up into the North Star and is explained by granular metrics like conversion_rate in the Metric Dependency Tree.",kbSlug:"metric-types"},hint:"SUM(amount) with an alias: AS deals_closed_value."},{slug:"sql-revenue-generated",chapter:1,title:"North Star metric: revenue_generated",difficulty:"warmup",prompt:"The `transactions` table records actual money movements (initial, renewal, upsell) against won deals.\n\nCompute **revenue_generated**: the total `amount` across all transactions. One row, one column named `revenue_generated`.",starterQuery:`SELECT -- aggregate here
FROM transactions;`,solution:"SELECT SUM(amount) AS revenue_generated FROM transactions;",dpmConnection:{text:"revenue_generated is the North Star for Sales. Notice it is NOT the same number as deals_closed_value: transactions include renewals and upsells that deal amounts don't. Chapter 6 is entirely about explaining that gap to the CFO.",kbSlug:"metric-types"},hint:"SUM over transactions.amount; alias as revenue_generated."},{slug:"sql-conversion-rate",chapter:1,title:"Granular metric: conversion_rate (Dana's definition)",difficulty:"warmup",prompt:"Compute **conversion_rate** = closed_won ÷ (closed_won + closed_lost). Open deals (prospecting, qualified, proposal, negotiation) are excluded: they haven't converted or failed yet.\n\nOne row, one column `conversion_rate`, rounded to 4 decimals.",starterQuery:`SELECT ROUND(
  1.0 * SUM(CASE WHEN stage = 'closed_won' THEN 1 ELSE 0 END)
  / SUM(CASE WHEN stage IN ('closed_won', 'closed_lost') THEN 1 ELSE 0 END),
  4
) AS conversion_rate
FROM deals;`,solution:"SELECT ROUND(1.0 * SUM(CASE WHEN stage = 'closed_won' THEN 1 ELSE 0 END) / SUM(CASE WHEN stage IN ('closed_won', 'closed_lost') THEN 1 ELSE 0 END), 4) AS conversion_rate FROM deals;",dpmConnection:{text:"Compare this with the 'won ÷ all deals' query Raj showed you in the quiz. Same table, different denominator, a very different number. Which one is 'right' is a definition decision, and it belongs on the Metric Dependency Tree with a name.",kbSlug:"metric-types"},hint:"The starter is complete. Read it: 1.0 * forces decimal division; CASE WHEN inside SUM counts conditionally."},{slug:"sql-join-accounts-deals",chapter:2,title:"Won value by account (JOIN + GROUP BY)",difficulty:"core",prompt:"Join `accounts` to `deals` and report, for each account, the **account_name** and its **total_won** (sum of `amount` for `closed_won` deals). Order by `total_won` descending, then `account_name`.",starterQuery:`SELECT a.account_name, SUM(d.amount) AS total_won
FROM accounts a
JOIN deals d ON -- join condition
WHERE d.stage = 'closed_won'
GROUP BY a.account_name
ORDER BY total_won DESC, a.account_name;`,solution:"SELECT a.account_name, SUM(d.amount) AS total_won FROM accounts a JOIN deals d ON a.account_id = d.account_id WHERE d.stage = 'closed_won' GROUP BY a.account_name ORDER BY total_won DESC, a.account_name;",orderMatters:!0,dpmConnection:{text:"This is the logical model's 'Relationships' (accounts 1:N deals) turned into a measure. Splitting a functional metric by an entity is the first move of any root-cause walk.",kbSlug:"canvas-data-product-design"},hint:"Join on a.account_id = d.account_id."},{slug:"sql-rep-performance",chapter:2,title:"Won value by rep",difficulty:"core",prompt:"For each sales rep (`owner`), sum the `amount` of their `closed_won` deals. Return **owner** and **total_won**, ordered by `total_won` descending.",starterQuery:`SELECT owner, SUM(amount) AS total_won
FROM deals
WHERE stage = 'closed_won'
GROUP BY -- ?
ORDER BY total_won DESC;`,solution:"SELECT owner, SUM(amount) AS total_won FROM deals WHERE stage = 'closed_won' GROUP BY owner ORDER BY total_won DESC;",orderMatters:!0,dpmConnection:{text:"Slicing a metric by a dimension (owner) is what Dana actually asks for in a pipeline review. As a Data PM, look at the concentration: if one rep is a large share of the number, a data problem on their deals moves the North Star.",kbSlug:"bullseye-data-product-market-fit"},hint:"GROUP BY owner."},{slug:"sql-region-conversion",chapter:2,title:"conversion_rate by region",difficulty:"core",prompt:"Compute Dana's **conversion_rate** per **region** (region lives on `accounts`, stage on `deals`). Round to 4 decimals, order by region.",starterQuery:`SELECT a.region,
  ROUND(
    1.0 * SUM(CASE WHEN d.stage = 'closed_won' THEN 1 ELSE 0 END)
    / SUM(CASE WHEN d.stage IN ('closed_won', 'closed_lost') THEN 1 ELSE 0 END),
    4
  ) AS conversion_rate
FROM deals d
JOIN accounts a ON a.account_id = d.account_id
GROUP BY a.region
ORDER BY a.region;`,solution:"SELECT a.region, ROUND(1.0 * SUM(CASE WHEN d.stage = 'closed_won' THEN 1 ELSE 0 END) / SUM(CASE WHEN d.stage IN ('closed_won', 'closed_lost') THEN 1 ELSE 0 END), 4) AS conversion_rate FROM deals d JOIN accounts a ON a.account_id = d.account_id GROUP BY a.region ORDER BY a.region;",orderMatters:!0,dpmConnection:{text:"A granular metric cut by a dimension from a *different* entity. Most real metric-tree questions have this shape: the measure on one table, the slicing attribute on another. The join is where silent errors creep in.",kbSlug:"canvas-data-product-design"},hint:"The starter is complete. Understand each clause, then run it."},{slug:"sql-rca-source-lost",chapter:2,title:"Where are we losing deals?",difficulty:"core",prompt:"For each deal `source`, return **lost_value** (sum of `amount` for `closed_lost` deals) and **lost_count**. Order by `lost_value` descending.",starterQuery:`SELECT source,
  SUM(amount) AS lost_value,
  COUNT(*)    AS lost_count
FROM deals
WHERE stage = 'closed_lost'
GROUP BY source
ORDER BY lost_value DESC;`,solution:"SELECT source, SUM(amount) AS lost_value, COUNT(*) AS lost_count FROM deals WHERE stage = 'closed_lost' GROUP BY source ORDER BY lost_value DESC;",orderMatters:!0,dpmConnection:{text:"Two aggregates over the same filtered rows. The output is the raw material for a stakeholder conversation; the Data PM's job starts *after* the query: is Outbound losing on lead quality, pricing, or a data problem in how stage is recorded?",kbSlug:"proof-of-value-performance"},hint:"SUM for value, COUNT(*) for count, both over the closed_lost rows."},{slug:"sql-conversion-two-ways",chapter:2,title:"Two conversion rates, one query (CTE)",difficulty:"core",prompt:"Dana counts deals; Tomás counts dollars. Compute both on the same **resolved** deals (closed_won or closed_lost):\n\n- `conversion_rate_closed` = won deals ÷ resolved deals\n- `conversion_rate_value` = won amount ÷ resolved amount\n\nUse a CTE named `resolved` for the filtered rows. One row, both columns, rounded to 4 decimals.",starterQuery:`WITH resolved AS (
  SELECT * FROM deals WHERE stage IN ('closed_won', 'closed_lost')
)
SELECT
  ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate_closed,
  -- add the value-based rate
FROM resolved;`,solution:"WITH resolved AS (SELECT * FROM deals WHERE stage IN ('closed_won', 'closed_lost')) SELECT ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate_closed, ROUND(1.0 * SUM(CASE WHEN stage = 'closed_won' THEN amount ELSE 0 END) / SUM(amount), 4) AS conversion_rate_value FROM resolved;",dpmConnection:{text:"Same rows, two legitimate metrics. The Metric Dependency Tree gives each a name and an owner so the whiteboard fight happens once. In SQLite, SUM(stage = 'closed_won') counts rows where the comparison is true.",kbSlug:"canvas-data-product-design"},hint:"SUM(CASE WHEN stage = 'closed_won' THEN amount ELSE 0 END) / SUM(amount)."},{slug:"sql-stage-funnel",chapter:2,title:"The funnel from the history log",difficulty:"core",prompt:"`deals.stage` is overwritten every time a deal moves. `deal_stage_history` keeps every transition.\n\nFor each stage, count the **distinct deals that ever entered it**. Return `stage` and `deals_reached`, ordered in funnel order (prospecting → qualified → proposal → negotiation → closed_won → closed_lost).",starterQuery:`SELECT stage, COUNT(DISTINCT deal_id) AS deals_reached
FROM deal_stage_history
GROUP BY stage
ORDER BY ${t};`,solution:`SELECT stage, COUNT(DISTINCT deal_id) AS deals_reached FROM deal_stage_history GROUP BY stage ORDER BY ${t};`,orderMatters:!0,dpmConnection:{text:"A funnel is an 'ever reached' question, which only an append-only log can answer. This is why the logical model treats the history table, not the CRM's current stage, as the source for funnel metrics.",kbSlug:"funnel-conversion-analysis"},hint:"COUNT(DISTINCT deal_id) so duplicate log rows don't inflate the count. The CASE in ORDER BY imposes funnel order."},{slug:"sql-stage-to-stage",chapter:2,title:"Stage-to-stage conversion (self-join on a CTE)",difficulty:"advanced",prompt:"Compute the conversion between consecutive funnel stages: prospecting→qualified, qualified→proposal, proposal→negotiation, negotiation→closed_won.\n\nBuild a CTE `reached` with each stage's distinct deal count and its funnel position (1–5, ignore closed_lost), then join it to itself on position + 1. Return `from_stage`, `to_stage`, `step_rate` (rounded to 4), in funnel order.",starterQuery:`WITH reached AS (
  SELECT stage,
    COUNT(DISTINCT deal_id) AS n,
    CASE stage WHEN 'prospecting' THEN 1 WHEN 'qualified' THEN 2
               WHEN 'proposal' THEN 3 WHEN 'negotiation' THEN 4
               WHEN 'closed_won' THEN 5 END AS ord
  FROM deal_stage_history
  WHERE stage <> 'closed_lost'
  GROUP BY stage
)
SELECT a.stage AS from_stage, b.stage AS to_stage,
  -- step_rate = b.n / a.n
FROM reached a
JOIN reached b ON b.ord = a.ord + 1
ORDER BY a.ord;`,solution:"WITH reached AS (SELECT stage, COUNT(DISTINCT deal_id) AS n, CASE stage WHEN 'prospecting' THEN 1 WHEN 'qualified' THEN 2 WHEN 'proposal' THEN 3 WHEN 'negotiation' THEN 4 WHEN 'closed_won' THEN 5 END AS ord FROM deal_stage_history WHERE stage <> 'closed_lost' GROUP BY stage) SELECT a.stage AS from_stage, b.stage AS to_stage, ROUND(1.0 * b.n / a.n, 4) AS step_rate FROM reached a JOIN reached b ON b.ord = a.ord + 1 ORDER BY a.ord;",orderMatters:!0,dpmConnection:{text:"Overall conversion says *whether* deals close; stage-to-stage says *where* they drop. Different question, different query. The Data PM reads the weakest step as the place to look first.",kbSlug:"funnel-conversion-analysis"},hint:"ROUND(1.0 * b.n / a.n, 4) AS step_rate."},{slug:"sql-days-in-stage",chapter:2,title:"Average days in each stage (LEAD window)",difficulty:"advanced",prompt:"Velocity: how long do deals sit in each stage?\n\nFor every history row, the time in that stage is the gap to the deal's *next* row. Use `LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at)` and `julianday()` to get days. Start from `SELECT DISTINCT deal_id, stage, entered_at` so duplicate log rows don't create zero-day gaps.\n\nReturn `stage` and `avg_days` (rounded to 1 decimal) for the four pre-close stages, in funnel order.",starterQuery:`WITH h AS (
  SELECT DISTINCT deal_id, stage, entered_at FROM deal_stage_history
),
steps AS (
  SELECT deal_id, stage,
    julianday(LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at))
      - julianday(entered_at) AS days
  FROM h
)
SELECT stage, ROUND(AVG(days), 1) AS avg_days
FROM steps
WHERE days IS NOT NULL
GROUP BY stage
ORDER BY CASE stage WHEN 'prospecting' THEN 1 WHEN 'qualified' THEN 2 WHEN 'proposal' THEN 3 ELSE 4 END;`,solution:"WITH h AS (SELECT DISTINCT deal_id, stage, entered_at FROM deal_stage_history), steps AS (SELECT deal_id, stage, julianday(LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at)) - julianday(entered_at) AS days FROM h) SELECT stage, ROUND(AVG(days), 1) AS avg_days FROM steps WHERE days IS NOT NULL GROUP BY stage ORDER BY CASE stage WHEN 'prospecting' THEN 1 WHEN 'qualified' THEN 2 WHEN 'proposal' THEN 3 ELSE 4 END;",orderMatters:!0,dpmConnection:{text:"Your first window function. Terminal stages get NULL from LEAD (no next row), which the WHERE removes. Time-in-stage is the metric Dana actually wants when she says 'where are deals stuck?'",kbSlug:"sql-toolkit-for-data-pms"},hint:"The starter is complete; run it and make sure you can explain why closed stages don't appear."},{slug:"sql-monthly-cohort",chapter:2,title:"Created-month cohorts",difficulty:"core",prompt:"Group deals by the month they were created (`substr(created_date, 1, 7)`). Return `cohort_month`, `created`, `won`, `lost`, `still_open`, ordered by month.",starterQuery:`SELECT substr(created_date, 1, 7) AS cohort_month,
  COUNT(*) AS created,
  SUM(stage = 'closed_won') AS won,
  -- lost, still_open
FROM deals
GROUP BY cohort_month
ORDER BY cohort_month;`,solution:"SELECT substr(created_date, 1, 7) AS cohort_month, COUNT(*) AS created, SUM(stage = 'closed_won') AS won, SUM(stage = 'closed_lost') AS lost, SUM(stage NOT IN ('closed_won', 'closed_lost')) AS still_open FROM deals GROUP BY cohort_month ORDER BY cohort_month;",orderMatters:!0,dpmConnection:{text:"Look at the right edge: recent cohorts have many still_open deals. Any 'won ÷ created' chart will sag there for no business reason. Chapter 5 starts by ruling that artifact out.",kbSlug:"funnel-conversion-analysis"},hint:"SUM(stage = 'closed_lost') and SUM(stage NOT IN ('closed_won','closed_lost'))."},{slug:"sql-dup-transactions",chapter:3,title:"Uniqueness: duplicate transactions",difficulty:"core",prompt:"The revenue tile jumped overnight and fell back. Find transactions that appear more than once with the same `deal_id`, `amount`, and `transaction_date`.\n\nReturn `deal_id`, `amount`, `transaction_date`, `copies` (the count), ordered by `deal_id`.",starterQuery:`SELECT deal_id, amount, transaction_date, COUNT(*) AS copies
FROM transactions
GROUP BY deal_id, amount, transaction_date
HAVING -- only groups with more than one row
ORDER BY deal_id;`,solution:"SELECT deal_id, amount, transaction_date, COUNT(*) AS copies FROM transactions GROUP BY deal_id, amount, transaction_date HAVING COUNT(*) > 1 ORDER BY deal_id;",orderMatters:!0,dpmConnection:{text:"GROUP BY key HAVING COUNT(*) > 1 is *the* duplicate idiom. A re-ingested file passes every pipeline status check and still double-counts revenue. This query becomes a uniqueness SLO that runs after every load.",kbSlug:"data-quality-dimensions"},hint:"HAVING COUNT(*) > 1."},{slug:"sql-stale-stage",chapter:3,title:"Consistency: CRM stage vs. latest history",difficulty:"advanced",prompt:"Reps update stages late. Find deals whose `deals.stage` disagrees with the **latest** row in `deal_stage_history`.\n\nUse `ROW_NUMBER() OVER (PARTITION BY deal_id ORDER BY entered_at DESC, history_id DESC)` in a CTE to pick each deal's latest history row. Return `deal_id`, `crm_stage`, `latest_stage`, ordered by `deal_id`.",starterQuery:`WITH latest AS (
  SELECT deal_id, stage,
    ROW_NUMBER() OVER (PARTITION BY deal_id ORDER BY entered_at DESC, history_id DESC) AS rn
  FROM deal_stage_history
)
SELECT d.deal_id, d.stage AS crm_stage, l.stage AS latest_stage
FROM deals d
JOIN latest l ON l.deal_id = d.deal_id AND l.rn = 1
WHERE -- the two stages differ
ORDER BY d.deal_id;`,solution:"WITH latest AS (SELECT deal_id, stage, ROW_NUMBER() OVER (PARTITION BY deal_id ORDER BY entered_at DESC, history_id DESC) AS rn FROM deal_stage_history) SELECT d.deal_id, d.stage AS crm_stage, l.stage AS latest_stage FROM deals d JOIN latest l ON l.deal_id = d.deal_id AND l.rn = 1 WHERE d.stage <> l.stage ORDER BY d.deal_id;",orderMatters:!0,dpmConnection:{text:"'Latest row per entity' via ROW_NUMBER is the most reused window pattern in data work. The fix isn't editing four CRM rows; it's making the history log the source of truth in the transform and turning this query into a consistency SLO.",kbSlug:"data-quality-dimensions"},hint:"WHERE d.stage <> l.stage."},{slug:"sql-missing-closed-date",chapter:3,title:"Completeness: closed deals without a close date",difficulty:"core",prompt:"Every closed deal must have a `closed_date`. Find the ones that don't. Return `deal_id`, `stage`, `owner`, ordered by `deal_id`.",starterQuery:`SELECT deal_id, stage, owner
FROM deals
WHERE stage IN ('closed_won', 'closed_lost')
  AND -- closed_date is missing
ORDER BY deal_id;`,solution:"SELECT deal_id, stage, owner FROM deals WHERE stage IN ('closed_won', 'closed_lost') AND closed_date IS NULL ORDER BY deal_id;",orderMatters:!0,dpmConnection:{text:"NULL never equals anything; you must write IS NULL. These rows silently vanish from any 'closed in month X' report, so bookings by month are understated without anyone noticing. Completeness SLO: 100% of closed deals have a closed_date.",kbSlug:"data-quality-dimensions"},hint:"closed_date IS NULL (not = NULL)."},{slug:"sql-freshness",chapter:3,title:"Timeliness: how stale is each pipeline?",difficulty:"core",prompt:"For each pipeline, find the most recent run that did **not** fail (`status <> 'failed'`) and how many hours old it is as of `2026-08-31 09:00:00`.\n\nReturn `pipeline`, `last_good_run` (max `finished_at`), `hours_stale` (rounded to 1 decimal, using `julianday` × 24), ordered by pipeline.",starterQuery:`SELECT pipeline,
  MAX(finished_at) AS last_good_run,
  ROUND((julianday('2026-08-31 09:00:00') - julianday(MAX(finished_at))) * 24, 1) AS hours_stale
FROM pipeline_runs
WHERE status <> 'failed'
GROUP BY pipeline
ORDER BY pipeline;`,solution:"SELECT pipeline, MAX(finished_at) AS last_good_run, ROUND((julianday('2026-08-31 09:00:00') - julianday(MAX(finished_at))) * 24, 1) AS hours_stale FROM pipeline_runs WHERE status <> 'failed' GROUP BY pipeline ORDER BY pipeline;",orderMatters:!0,dpmConnection:{text:"This is a freshness SLO as a query: measurement, threshold (< 24h), and evaluation time (09:00). 'The job is scheduled at 02:00' is not an SLO; this is.",kbSlug:"data-quality-dimensions"},hint:"The starter is complete. Change the as-of timestamp and watch hours_stale move."},{slug:"sql-failed-recovery",chapter:3,title:"Failed runs and time to recover",difficulty:"advanced",prompt:"For every failed run, find when that pipeline next had a non-failed run and how many days that took.\n\nReturn `pipeline`, `failed_on`, `recovered_on`, `days_to_recover` (integer), ordered by pipeline then `failed_on`. A correlated subquery is the simplest tool here.",starterQuery:`SELECT f.pipeline,
  f.run_date AS failed_on,
  (SELECT MIN(s.run_date) FROM pipeline_runs s
    WHERE s.pipeline = f.pipeline AND s.status <> 'failed' AND s.run_date > f.run_date) AS recovered_on,
  -- days_to_recover: julianday(recovered_on) - julianday(failed_on), cast to INTEGER
FROM pipeline_runs f
WHERE f.status = 'failed'
ORDER BY f.pipeline, f.run_date;`,solution:"SELECT f.pipeline, f.run_date AS failed_on, (SELECT MIN(s.run_date) FROM pipeline_runs s WHERE s.pipeline = f.pipeline AND s.status <> 'failed' AND s.run_date > f.run_date) AS recovered_on, CAST(julianday((SELECT MIN(s.run_date) FROM pipeline_runs s WHERE s.pipeline = f.pipeline AND s.status <> 'failed' AND s.run_date > f.run_date)) - julianday(f.run_date) AS INTEGER) AS days_to_recover FROM pipeline_runs f WHERE f.status = 'failed' ORDER BY f.pipeline, f.run_date;",orderMatters:!0,dpmConnection:{text:"A three-day gap with no alert is how the June revenue numbers went stale for a weekend. Time-to-recover is the SLO metric an on-call rotation is measured on; you can't manage it until you can query it.",kbSlug:"data-product-activation"},hint:"Repeat the subquery inside CAST(julianday(...) - julianday(f.run_date) AS INTEGER), or wrap the whole thing in a CTE."},{slug:"sql-rowcount-anomaly",chapter:3,title:"Row-count anomaly vs. trailing average (window frame)",difficulty:"advanced",prompt:"A run can succeed and still process a partial file. Flag non-failed runs whose `rows_out` is below **50% of the average of the previous 7 runs** for the same pipeline.\n\nUse `AVG(rows_out) OVER (PARTITION BY pipeline ORDER BY run_date ROWS BETWEEN 7 PRECEDING AND 1 PRECEDING)`. Return `pipeline`, `run_date`, `rows_out`, `trailing_avg` (rounded to 1), ordered by `run_date`.",starterQuery:`WITH ok AS (
  SELECT pipeline, run_date, rows_out FROM pipeline_runs WHERE status <> 'failed'
),
w AS (
  SELECT pipeline, run_date, rows_out,
    AVG(rows_out) OVER (
      PARTITION BY pipeline ORDER BY run_date
      ROWS BETWEEN 7 PRECEDING AND 1 PRECEDING
    ) AS trailing_avg
  FROM ok
)
SELECT pipeline, run_date, rows_out, ROUND(trailing_avg, 1) AS trailing_avg
FROM w
WHERE trailing_avg IS NOT NULL AND -- rows_out is less than half the trailing average
ORDER BY run_date;`,solution:"WITH ok AS (SELECT pipeline, run_date, rows_out FROM pipeline_runs WHERE status <> 'failed'), w AS (SELECT pipeline, run_date, rows_out, AVG(rows_out) OVER (PARTITION BY pipeline ORDER BY run_date ROWS BETWEEN 7 PRECEDING AND 1 PRECEDING) AS trailing_avg FROM ok) SELECT pipeline, run_date, rows_out, ROUND(trailing_avg, 1) AS trailing_avg FROM w WHERE trailing_avg IS NOT NULL AND rows_out < 0.5 * trailing_avg ORDER BY run_date;",orderMatters:!0,dpmConnection:{text:"A window *frame* (ROWS BETWEEN … PRECEDING) turns a per-run number into a per-run baseline. This is the volume check that catches 'success' on a 30% file, which no status column ever will.",kbSlug:"data-quality-dimensions"},hint:"rows_out < 0.5 * trailing_avg."},{slug:"sql-weekly-active",chapter:4,title:"Weekly active viewers",difficulty:"core",prompt:"Count **distinct viewers per week** of the *Sales Funnel Accelerator* dashboard. Bucket weeks with `strftime('%Y-%W', viewed_at)`. Return `week` and `active_viewers`, ordered by week.",starterQuery:`SELECT strftime('%Y-%W', viewed_at) AS week,
  -- distinct people, not rows
FROM dashboard_views
WHERE dashboard = 'Sales Funnel Accelerator'
GROUP BY week
ORDER BY week;`,solution:"SELECT strftime('%Y-%W', viewed_at) AS week, COUNT(DISTINCT viewer_id) AS active_viewers FROM dashboard_views WHERE dashboard = 'Sales Funnel Accelerator' GROUP BY week ORDER BY week;",orderMatters:!0,dpmConnection:{text:"COUNT(DISTINCT viewer_id), never COUNT(*): views measure activity, viewers measure adoption. Sofia's question was 'is anyone using it?', which is a question about people.",kbSlug:"go-to-market-launch"},hint:"COUNT(DISTINCT viewer_id) AS active_viewers."},{slug:"sql-adoption-by-role",chapter:4,title:"Adoption by role",difficulty:"core",prompt:"For the *Sales Funnel Accelerator*, report per `viewer_role`: `viewers` (distinct people), `views` (rows), and `views_per_viewer` (rounded to 1 decimal). Order by `viewers` descending, then role.",starterQuery:`SELECT viewer_role,
  COUNT(DISTINCT viewer_id) AS viewers,
  COUNT(*) AS views,
  -- views_per_viewer
FROM dashboard_views
WHERE dashboard = 'Sales Funnel Accelerator'
GROUP BY viewer_role
ORDER BY viewers DESC, viewer_role;`,solution:"SELECT viewer_role, COUNT(DISTINCT viewer_id) AS viewers, COUNT(*) AS views, ROUND(1.0 * COUNT(*) / COUNT(DISTINCT viewer_id), 1) AS views_per_viewer FROM dashboard_views WHERE dashboard = 'Sales Funnel Accelerator' GROUP BY viewer_role ORDER BY viewers DESC, viewer_role;",orderMatters:!0,dpmConnection:{text:"Which audience is missing is more useful than how many views there are. A role with zero viewers (Finance, at Meridian) is next week's stakeholder conversation, not a failure of the dashboard.",kbSlug:"go-to-market-launch"},hint:"ROUND(1.0 * COUNT(*) / COUNT(DISTINCT viewer_id), 1)."},{slug:"sql-dashboard-mom",chapter:4,title:"Legacy vs. new dashboard, month over month (LAG)",difficulty:"advanced",prompt:"For each dashboard and month, report `views` and the change versus the previous month (`change_vs_prev`, NULL for the first month). Aggregate to months in a CTE first, then use `LAG(views) OVER (PARTITION BY dashboard ORDER BY month)`. Order by dashboard, month.",starterQuery:`WITH m AS (
  SELECT dashboard, substr(viewed_at, 1, 7) AS month, COUNT(*) AS views
  FROM dashboard_views
  GROUP BY dashboard, month
)
SELECT dashboard, month, views,
  -- views minus the previous month's views for the same dashboard
FROM m
ORDER BY dashboard, month;`,solution:"WITH m AS (SELECT dashboard, substr(viewed_at, 1, 7) AS month, COUNT(*) AS views FROM dashboard_views GROUP BY dashboard, month) SELECT dashboard, month, views, views - LAG(views) OVER (PARTITION BY dashboard ORDER BY month) AS change_vs_prev FROM m ORDER BY dashboard, month;",orderMatters:!0,dpmConnection:{text:"Grain first (one row per dashboard-month), window second. The story in the output, legacy fading while the new product grows, is the evidence that lets you retire the old report with a redirect instead of a memo.",kbSlug:"sql-toolkit-for-data-pms"},hint:"views - LAG(views) OVER (PARTITION BY dashboard ORDER BY month) AS change_vs_prev."},{slug:"sql-rca-confirm",chapter:5,title:"Step 1: is the drop real?",difficulty:"core",prompt:"Before slicing, remove the open-deal artifact. Compute conversion (won ÷ resolved) by **created-month cohort**, using only **resolved** deals created on or before `2026-06-30`.\n\nReturn `cohort_month`, `resolved`, `conversion_rate` (4 decimals), ordered by month.",starterQuery:`SELECT substr(created_date, 1, 7) AS cohort_month,
  COUNT(*) AS resolved,
  ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate
FROM deals
WHERE stage IN ('closed_won', 'closed_lost')
  AND created_date <= '2026-06-30'
GROUP BY cohort_month
ORDER BY cohort_month;`,solution:"SELECT substr(created_date, 1, 7) AS cohort_month, COUNT(*) AS resolved, ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate FROM deals WHERE stage IN ('closed_won', 'closed_lost') AND created_date <= '2026-06-30' GROUP BY cohort_month ORDER BY cohort_month;",orderMatters:!0,dpmConnection:{text:"The first RCA step is proving the number is real, not an artifact of cohorts that haven't had time to close. May and June should still look low after this filter. If they didn't, the meeting would be over.",kbSlug:"funnel-conversion-analysis"},hint:"The starter is complete. Compare with the unfiltered cohort query from chapter 2."},{slug:"sql-rca-segment",chapter:5,title:"Step 2: which segment moved? (region × source, before vs. during)",difficulty:"advanced",prompt:"Compare conversion for deals created **before May** (`created_date < '2026-05-01'`) with deals created **May–June**, for every `region` × `source` cell. Resolved deals only, created on or before `2026-06-30`.\n\nReturn `region`, `source`, `conv_before`, `conv_during` (both rounded to 2 decimals), `deals_during`. Order by region, source.",starterQuery:`WITH r AS (
  SELECT d.stage, d.source, a.region,
    CASE WHEN d.created_date < '2026-05-01' THEN 'before' ELSE 'during' END AS period
  FROM deals d
  JOIN accounts a ON a.account_id = d.account_id
  WHERE d.stage IN ('closed_won', 'closed_lost')
    AND d.created_date <= '2026-06-30'
)
SELECT region, source,
  ROUND(1.0 * SUM(period = 'before' AND stage = 'closed_won') / SUM(period = 'before'), 2) AS conv_before,
  -- conv_during, deals_during
FROM r
GROUP BY region, source
ORDER BY region, source;`,solution:"WITH r AS (SELECT d.stage, d.source, a.region, CASE WHEN d.created_date < '2026-05-01' THEN 'before' ELSE 'during' END AS period FROM deals d JOIN accounts a ON a.account_id = d.account_id WHERE d.stage IN ('closed_won', 'closed_lost') AND d.created_date <= '2026-06-30') SELECT region, source, ROUND(1.0 * SUM(period = 'before' AND stage = 'closed_won') / SUM(period = 'before'), 2) AS conv_before, ROUND(1.0 * SUM(period = 'during' AND stage = 'closed_won') / SUM(period = 'during'), 2) AS conv_during, SUM(period = 'during') AS deals_during FROM r GROUP BY region, source ORDER BY region, source;",orderMatters:!0,dpmConnection:{text:"One query, nine cells, and only one of them collapses. A price increase would depress every cell; a vendor change for AMER outbound leads depresses exactly one. This is how the tree kills theories cheaply.",kbSlug:"funnel-conversion-analysis"},hint:"Mirror conv_before with period = 'during'; deals_during is SUM(period = 'during')."},{slug:"sql-rca-velocity",chapter:5,title:"Step 3: where in the funnel? (negotiation time, before vs. during)",difficulty:"advanced",prompt:"In the AMER × Outbound segment, compare average days spent in **negotiation** for deals created before May versus May–June (created on or before `2026-06-30`).\n\nReuse the LEAD-based `steps` CTE from chapter 2. Return `period`, `avg_days_in_negotiation` (1 decimal), `deals`, ordered by period.",starterQuery:`WITH h AS (
  SELECT DISTINCT deal_id, stage, entered_at FROM deal_stage_history
),
steps AS (
  SELECT deal_id, stage,
    julianday(LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at))
      - julianday(entered_at) AS days
  FROM h
),
seg AS (
  SELECT d.deal_id,
    CASE WHEN d.created_date < '2026-05-01' THEN 'before' ELSE 'during' END AS period
  FROM deals d
  JOIN accounts a ON a.account_id = d.account_id
  WHERE a.region = 'AMER' AND d.source = 'Outbound' AND d.created_date <= '2026-06-30'
)
SELECT seg.period,
  ROUND(AVG(steps.days), 1) AS avg_days_in_negotiation,
  COUNT(*) AS deals
FROM steps
JOIN seg ON seg.deal_id = steps.deal_id
WHERE steps.stage = 'negotiation' AND steps.days IS NOT NULL
GROUP BY seg.period
ORDER BY seg.period;`,solution:"WITH h AS (SELECT DISTINCT deal_id, stage, entered_at FROM deal_stage_history), steps AS (SELECT deal_id, stage, julianday(LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at)) - julianday(entered_at) AS days FROM h), seg AS (SELECT d.deal_id, CASE WHEN d.created_date < '2026-05-01' THEN 'before' ELSE 'during' END AS period FROM deals d JOIN accounts a ON a.account_id = d.account_id WHERE a.region = 'AMER' AND d.source = 'Outbound' AND d.created_date <= '2026-06-30') SELECT seg.period, ROUND(AVG(steps.days), 1) AS avg_days_in_negotiation, COUNT(*) AS deals FROM steps JOIN seg ON seg.deal_id = steps.deal_id WHERE steps.stage = 'negotiation' AND steps.days IS NOT NULL GROUP BY seg.period ORDER BY seg.period;",orderMatters:!0,dpmConnection:{text:"Deals still reach negotiation, then stall and die. Late-funnel losses with longer negotiation point at lead quality, not at reps' data hygiene. Three CTEs, each named for what it produces, is how a Data PM keeps a query like this readable in a meeting.",kbSlug:"funnel-conversion-analysis"},hint:"The starter is complete. Change the region/source filter to see that other segments did not change."},{slug:"sql-rca-size",chapter:5,title:"Step 4: size it",difficulty:"core",prompt:"Sofia needs a number. For AMER × Outbound deals created May–June 2026 that were **closed_lost**, return `owner`, `lost_deals`, `lost_value`, ordered by `lost_value` descending.",starterQuery:`SELECT d.owner, COUNT(*) AS lost_deals, SUM(d.amount) AS lost_value
FROM deals d
JOIN accounts a ON a.account_id = d.account_id
WHERE a.region = 'AMER' AND d.source = 'Outbound'
  AND d.stage = 'closed_lost'
  AND d.created_date BETWEEN '2026-05-01' AND '2026-06-30'
GROUP BY d.owner
ORDER BY lost_value DESC;`,solution:"SELECT d.owner, COUNT(*) AS lost_deals, SUM(d.amount) AS lost_value FROM deals d JOIN accounts a ON a.account_id = d.account_id WHERE a.region = 'AMER' AND d.source = 'Outbound' AND d.stage = 'closed_lost' AND d.created_date BETWEEN '2026-05-01' AND '2026-06-30' GROUP BY d.owner ORDER BY lost_value DESC;",orderMatters:!0,dpmConnection:{text:"Root cause work ends with a size and an owner, not a chart. This is the slide: cause, deals and dollars affected, alternatives ruled out, proposed fix.",kbSlug:"proof-of-value-performance"},hint:"The starter is complete. Try removing the region/source filter to see how much of total May–June loss this segment explains."},{slug:"sql-reconcile-bookings-revenue",chapter:6,title:"Bookings vs. revenue by month",difficulty:"advanced",prompt:"Tomás has two numbers. Put them side by side per month:\n\n- `bookings` = sum of `amount` for `closed_won` deals by `closed_date` month (skip NULL dates)\n- `revenue` = sum of `transactions.amount` by `transaction_date` month\n\nSome months exist in only one side, so build a `months` CTE with `UNION` and LEFT JOIN both. Return `month`, `bookings`, `revenue`, `difference` (revenue − bookings; use COALESCE so missing months show 0). Order by month.",starterQuery:`WITH b AS (
  SELECT substr(closed_date, 1, 7) AS month, SUM(amount) AS bookings
  FROM deals WHERE stage = 'closed_won' AND closed_date IS NOT NULL
  GROUP BY month
),
r AS (
  SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue
  FROM transactions GROUP BY month
),
months AS (
  SELECT month FROM b UNION SELECT month FROM r
)
SELECT m.month,
  COALESCE(b.bookings, 0) AS bookings,
  COALESCE(r.revenue, 0) AS revenue,
  -- difference
FROM months m
LEFT JOIN b ON b.month = m.month
LEFT JOIN r ON r.month = m.month
ORDER BY m.month;`,solution:"WITH b AS (SELECT substr(closed_date, 1, 7) AS month, SUM(amount) AS bookings FROM deals WHERE stage = 'closed_won' AND closed_date IS NOT NULL GROUP BY month), r AS (SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue FROM transactions GROUP BY month), months AS (SELECT month FROM b UNION SELECT month FROM r) SELECT m.month, COALESCE(b.bookings, 0) AS bookings, COALESCE(r.revenue, 0) AS revenue, COALESCE(r.revenue, 0) - COALESCE(b.bookings, 0) AS difference FROM months m LEFT JOIN b ON b.month = m.month LEFT JOIN r ON r.month = m.month ORDER BY m.month;",orderMatters:!0,dpmConnection:{text:"Neither number is wrong. Bookings is what Sales books; revenue is what Finance collects. The reconciliation is the bridge on the board slide, and the SQL pattern (UNION of keys, LEFT JOIN both sides) is how you compare any two metrics that should agree.",kbSlug:"ontology-as-infrastructure"},hint:"COALESCE(r.revenue, 0) - COALESCE(b.bookings, 0) AS difference."},{slug:"sql-revenue-bridge",chapter:6,title:"Explain the gap: revenue bridge",difficulty:"advanced",prompt:"Break `transactions` into buckets that explain the bookings-vs-revenue gap: `initial`, `renewal`, `upsell`, and `duplicate` (the extra copies you found in chapter 3).\n\nUse `ROW_NUMBER() OVER (PARTITION BY deal_id, amount, transaction_date, type ORDER BY transaction_id)`; rows with `rn > 1` are duplicates. Return `bucket`, `transactions`, `amount`, ordered by `amount` descending.",starterQuery:`WITH ranked AS (
  SELECT *,
    ROW_NUMBER() OVER (PARTITION BY deal_id, amount, transaction_date, type ORDER BY transaction_id) AS rn
  FROM transactions
)
SELECT CASE WHEN rn > 1 THEN 'duplicate' ELSE type END AS bucket,
  COUNT(*) AS transactions,
  SUM(amount) AS amount
FROM ranked
GROUP BY bucket
ORDER BY amount DESC;`,solution:"WITH ranked AS (SELECT *, ROW_NUMBER() OVER (PARTITION BY deal_id, amount, transaction_date, type ORDER BY transaction_id) AS rn FROM transactions) SELECT CASE WHEN rn > 1 THEN 'duplicate' ELSE type END AS bucket, COUNT(*) AS transactions, SUM(amount) AS amount FROM ranked GROUP BY bucket ORDER BY amount DESC;",orderMatters:!0,dpmConnection:{text:"Every dollar of the difference now has a name. 'Initial' should reconcile to bookings; renewals and upsells are the healthy part of the gap; duplicates are the part you remove. This is the bridge the CFO puts under the two numbers.",kbSlug:"ontology-as-infrastructure"},hint:"The starter is complete. Compare the initial bucket with total bookings from the previous exercise."},{slug:"sql-running-revenue",chapter:6,title:"Running revenue total",difficulty:"advanced",prompt:"Compute monthly revenue from `transactions` and a **running total** across months. Aggregate to months in a CTE first, then `SUM(revenue) OVER (ORDER BY month)`. Return `month`, `revenue`, `running_total`, ordered by month.",starterQuery:`WITH m AS (
  SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue
  FROM transactions
  GROUP BY month
)
SELECT month, revenue,
  -- running_total
FROM m
ORDER BY month;`,solution:"WITH m AS (SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue FROM transactions GROUP BY month) SELECT month, revenue, SUM(revenue) OVER (ORDER BY month) AS running_total FROM m ORDER BY month;",orderMatters:!0,dpmConnection:{text:"Grain first, window second. Skip the CTE and the running total steps up on every transaction row instead of every month. This query becomes Meridian's first gold metric with a written definition, a test, and an SLO.",kbSlug:"sql-toolkit-for-data-pms"},hint:"SUM(revenue) OVER (ORDER BY month) AS running_total."},{slug:"sql-capstone-metric-tree",chapter:6,title:"Capstone: the whole metric tree, one query",difficulty:"advanced",prompt:"For the months `2026-06`, `2026-07`, `2026-08`, produce the metric tree in one result:\n\n- `revenue_generated`: transactions by `transaction_date` month\n- `deals_closed_value`: won amount by `closed_date` month\n- `conversion_rate`: won ÷ (won + lost) among deals **closed** in that month (4 decimals)\n\nTwo CTEs (`rev`, `closed`) joined on month. Return `month`, `revenue_generated`, `deals_closed_value`, `conversion_rate`, ordered by month.",starterQuery:`WITH rev AS (
  SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue_generated
  FROM transactions GROUP BY month
),
closed AS (
  SELECT substr(closed_date, 1, 7) AS month,
    SUM(CASE WHEN stage = 'closed_won' THEN amount ELSE 0 END) AS deals_closed_value,
    ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate
  FROM deals
  WHERE closed_date IS NOT NULL AND stage IN ('closed_won', 'closed_lost')
  GROUP BY month
)
SELECT c.month,
  COALESCE(r.revenue_generated, 0) AS revenue_generated,
  c.deals_closed_value,
  c.conversion_rate
FROM closed c
LEFT JOIN rev r ON r.month = c.month
WHERE c.month IN ('2026-06', '2026-07', '2026-08')
ORDER BY c.month;`,solution:"WITH rev AS (SELECT substr(transaction_date, 1, 7) AS month, SUM(amount) AS revenue_generated FROM transactions GROUP BY month), closed AS (SELECT substr(closed_date, 1, 7) AS month, SUM(CASE WHEN stage = 'closed_won' THEN amount ELSE 0 END) AS deals_closed_value, ROUND(1.0 * SUM(stage = 'closed_won') / COUNT(*), 4) AS conversion_rate FROM deals WHERE closed_date IS NOT NULL AND stage IN ('closed_won', 'closed_lost') GROUP BY month) SELECT c.month, COALESCE(r.revenue_generated, 0) AS revenue_generated, c.deals_closed_value, c.conversion_rate FROM closed c LEFT JOIN rev r ON r.month = c.month WHERE c.month IN ('2026-06', '2026-07', '2026-08') ORDER BY c.month;",orderMatters:!0,dpmConnection:{text:"North Star, functional, granular: one definition each, one query, one owner. Six weeks ago these were three arguments. Now they are a semantic contract that a dashboard, a finance analyst, and an AI agent can all read the same way.",kbSlug:"metric-types"},hint:"The starter is complete. Notice conversion here is by *closed* month, not created cohort: a different, equally valid cut. Name it accordingly on the tree."}];e.s(["sqlExercises",0,a,"sqlExercisesForChapter",0,function(e){return a.filter(t=>t.chapter===e)}])},6e4,e=>{"use strict";var t=e.i(47371);let a=(e,t,a)=>({kind:"concept",title:e,body:t,kbSlug:a}),o=(e,t,a,o,n,s)=>({kind:"mcq",prompt:e,options:t,correct:a,explanation:o,optionNotes:n,kbSlug:s}),n=(e,t,a,o)=>({kind:"truefalse",statement:e,answer:t,explanation:a,kbSlug:o}),s=(e,t,a,o,n)=>({kind:"fill",prompt:e,bank:t,answer:a,explanation:o,kbSlug:n}),r=(e,t,a,o)=>({kind:"match",prompt:e,pairs:t,explanation:a,kbSlug:o}),i=(e,t,a,o)=>({kind:"order",prompt:e,steps:t,explanation:a,kbSlug:o});function d(e,t,a,o){return{id:e,unit:t,title:a,items:o.map((t,a)=>({...t,id:`${e}-${a+1}`}))}}let l=[d("u1-l1",1,"What a Data PM actually owns",[a("The role","A **Data Product Manager** sits where data, technology, and business meet. Instead of a feature, the product is a warehouse, a pipeline, a metric, or a model. The core job is **earning and protecting trust** in the numbers people decide with.","what-is-a-data-product-manager"),o("Which of these is a *data product*?",["A pricing page redesign","A sales-funnel metric with a written definition and an owner","A quarterly sales kickoff","A new CRM login flow"],1,"A metric with a definition, a pipeline behind it, and someone accountable for it is a data product. The others are software or events.",void 0,"what-is-a-data-product-manager"),n("A Data PM's main deliverable is a dashboard.",!1,"Dashboards are one output port. The deliverable is a trusted, defined, maintained data product; the dashboard is where people read it.","what-is-a-data-product-manager"),s("A Data PM sits at the intersection of data, technology, and ___.",["business","design","marketing","finance"],"business","Data + technology + business. The business side is what makes it product management rather than data engineering.","what-is-a-data-product-manager"),r("Match each thing to who usually owns it.",[["Roadmap and priorities","Data PM"],["Pipeline code","Analytics engineer"],["Metric definition sign-off","Data PM with the domain owner"],["CRM data entry","Sales reps"]],"The Data PM owns priorities and definitions; engineers own the code; the domain owns the raw data it creates.","what-is-a-data-product-manager"),o("A traditional PM leans on UX and go-to-market. A Data PM leans more on…",["Technical and data-specific judgment","Visual design","Paid advertising","Legal review"],0,"Data PMs are more technical: they reason about schemas, definitions, quality, and lineage. UX still matters for analytics products, but it's not the centre of gravity.",void 0,"what-is-a-data-product-manager")]),d("u1-l2",1,"The four key shifts",[a("Four things invert","Moving from general PM to Data PM flips four dials: **risk** (bad data burns trust for good), **success metrics** (quality and decision velocity, not engagement), the **customer journey** (starts with skepticism), and the **team question** (from *when can we ship?* to *how do we validate?*).","four-key-shifts"),o("A buggy feature vs. a bad number: what's the difference in risk?",["None, both get fixed","Bad data can lose trust permanently; a bug is usually recoverable with a fix","Bugs are worse because users see them","Bad data is cheaper to fix"],1,"The asymmetry is trust. A stakeholder burned once discounts every future number.",["Both get fixed, but only one leaves a lasting mark.",void 0,"Users see wrong numbers too, in the board deck.","Fixing the row is cheap; regaining trust is not."],"four-key-shifts"),r("Match the dimension to the Data PM version.",[["Success metrics","Data quality, decision velocity, trust"],["Customer journey",'"I don\'t trust this yet… but maybe"'],["Team question",'"How do we validate?"'],["Risk profile","Ship bad data, lose trust"]],"Those are the four shifts. Notice none of them is about tooling.","four-key-shifts"),n("For a Data PM, usage and engagement are the primary success metrics.",!1,"Those are general-PM metrics. A data product succeeds when its numbers are trusted and decisions get made faster on them.","four-key-shifts"),s('The Data PM team\'s default question changes from "when can we ship?" to "how do we ___?"',["validate","scale","market","monetize"],"validate","Validation before shipping: a data product that ships wrong is worse than one that ships late.","four-key-shifts"),o('Dana opens the new dashboard and says "I don\'t trust this yet." That reaction is…',["A sign the product failed","The normal starting point of the data-product journey","Something to escalate","Proof the numbers are wrong"],1,'Skepticism is the opening emotion for data products. Your job is to move it to "…but maybe" with definitions, checks, and transparency.',void 0,"four-key-shifts")]),d("u1-l3",1,"North Star, functional, granular",[a("The metric tree","Every domain needs three layers: a **North Star** tied to the business (revenue_generated), a **functional** metric for the team (deals_closed_value), and **granular** metrics that explain movement (conversion_rate, days_in_stage). They form a **Metric Dependency Tree**: granular → functional → North Star.","metric-types"),r("Match each Sales metric to its layer.",[["revenue_generated","North Star"],["deals_closed_value","Functional"],["conversion_rate","Granular"]],"Several granular metrics (rates, times in stage) can sit under one functional metric; they all explain why bookings moved.","metric-types"),o("What makes a metric *granular*?",["It is computed daily","It is a rate or ratio that explains *why* a higher-level metric moved","It is the smallest number on the dashboard","It comes from a single table"],1,"Granular metrics are indicators of effectiveness. When revenue drops, you walk down to them to find the cause.",void 0,"metric-types"),n("The North Star metric should be the one the CEO can act on directly.",!1,"The North Star ties the domain to business outcomes; it moves because functional and granular metrics move. Actions happen lower in the tree.","metric-types"),s("Granular metrics roll up into functional metrics, which roll up into the ___.",["North Star","dashboard","OKR","data warehouse"],"North Star","That roll-up is the Metric Dependency Tree, and it's what makes root-cause analysis a walk instead of a guess.","metric-types"),i("Put the tree in order, top to bottom.",["North Star: revenue_generated","Functional: deals_closed_value","Granular: conversion_rate"],"Top-down is how a stakeholder reads it; bottom-up is how the numbers are built.","metric-types")]),d("u1-l4",1,"Discovery before SQL",[a("Week 1: the Bullseye","Before building anything: **interview end users**, **map how they use data today**, **agree the problem statement** in their words, and **identify the metric tree**. Spend a full week here. The output is a problem statement two stakeholders sign, not a query.","bullseye-data-product-market-fit"),i("Put the Week 1 steps in order.",["Interview end users and capture pain points","Map the existing user journey","Agree the problem statement (value prop)","Identify North Star, functional, granular metrics"],"Requirements → journey → purpose → metrics. Metrics come last because they must serve the problem, not the other way round.","bullseye-data-product-market-fit"),o("Dana says the 61% conversion rate is wrong and wants it fixed by Friday. Raj says pipelines are green. Best first move?",["Debug the dashboard SQL","Ask Dana what number she expects, how she'd compute it by hand, and what decision it drives","Tell Dana the pipelines are green","Escalate to Sofia"],1,'Most "wrong numbers" are two definitions colliding. Pin the definition, the expected value, and the decision before touching code.',["You don't yet know which number or which definition.",void 0,"Green means the code ran, not that the metric means what she thinks.","Nothing is diagnosed yet."],"bullseye-data-product-market-fit"),n("If you already see the problem on Wednesday of week 1, it's fine to start building.",!1,"Two stakeholders you haven't met hold different definitions. Building now bakes in a fight you haven't had. Finish discovery.","bullseye-data-product-market-fit"),s("For a proof of value, pick metrics that are directly useful to ___.",["leadership","engineers","the vendor","auditors"],"leadership","A six-week proof of value has to move something a leader cares about, or it won't get a second six weeks.","bullseye-data-product-market-fit"),o("Which question belongs in Week 1 discovery?",["Which cloud warehouse should we buy?","Which persona would get the most value from this data, and what decision would they make with it?","Should the dashboard be dark mode?","Which chart library is fastest?"],1,"Discovery is about persona, pain, decision, and value. Tooling questions come after the problem statement.",void 0,"bullseye-data-product-market-fit")]),d("u1-l5",1,"Where your company sits",[a("Four maturity stages","**Ad-hoc** (no process) → **Reactive** (build what's requested) → **Strategy-driven** (chasing hype without users) → **Purpose-driven** (initiatives measured by a clear metric tree). Most companies are Reactive. A proof of value shows Purpose-driven on one narrow slice.","data-product-maturity-stages"),i("Order the maturity stages from least to most mature.",["Ad-hoc","Reactive","Strategy-driven","Purpose-driven"],"Reactive is the common default. Strategy-driven sounds better but often means a migration or a GenAI project with no user attached.","data-product-maturity-stages"),o("Raj builds whatever the last request was; nobody owns definitions. Which stage is Meridian in?",["Ad-hoc","Reactive","Strategy-driven","Purpose-driven"],1,"Requests in, pipelines out, no purpose or metric tree: textbook Reactive.",void 0,"data-product-maturity-stages"),n("A large data-platform migration automatically moves a company to Purpose-driven.",!1,"That's Strategy-driven at best: a big initiative without a concrete bridge to end users and a measured purpose.","data-product-maturity-stages"),o('What does "Purpose-driven" mean in practice?',["Every dataset is documented","Data initiatives are tied to a business purpose and measured by North Star, functional, and granular metrics","The data team reports to the CEO","All pipelines are real-time"],1,"Purpose plus measurement. Documentation and org charts are inputs, not the definition.",void 0,"data-product-maturity-stages"),s("The point of a 6-week proof of value is to demonstrate Purpose-driven on ___ slice, not to boil the ocean.",["one narrow","every","the largest","a random"],"one narrow","One domain, one metric tree, one set of users. Scale comes after proof.","data-product-maturity-stages")])],u=[d("u2-l1",2,"Three conversion rates",[a("Same word, three formulas","Dana: won ÷ (won + lost). Lena: won ÷ created. Tomás: won *amount* ÷ closed *amount*. All legitimate, all different charts. The Data PM's move is not to pick a winner in the meeting but to put each on the **Metric Dependency Tree** with a name, a formula, and an owner.","canvas-data-product-design"),o("Three stakeholders, three conversion formulas. What do you do?",["Pick the VP's","Average them","Name all three on the tree with formulas and owners; show one by default and label it","Ask which is easiest to compute"],2,"The definition fight should happen once and be written down. Different questions deserve different metrics; the sin is calling them the same thing.",["You'd win the meeting and lose Finance and RevOps.","A blended number nobody can reproduce destroys trust.",void 0,"Implementation cost isn't a reason to choose a business definition."],"canvas-data-product-design"),r("Match the formula to the person who wants it.",[["won ÷ (won + lost)","Dana (VP Sales)"],["won ÷ created","Lena (RevOps)"],["won amount ÷ closed amount","Tomás (CFO)"]],"Deal counts for sales management, created-based for the CRM report, dollars for finance. Each answers a real question.","canvas-data-product-design"),n("Including open deals in the denominator makes conversion drift down over time for no business reason.",!0,'Recent deals haven\'t had time to close, so "won ÷ created" sags at the right edge of any chart.',"funnel-conversion-analysis"),o("Raj's query: `SUM(stage = 'closed_won') / COUNT(*)` over all deals. Which definition is that?",["Dana's (won ÷ resolved)","Lena's (won ÷ created, open deals included)","Tomás's (value-based)","None; it errors"],1,"COUNT(*) counts every deal regardless of stage, so open deals sit in the denominator.",void 0,"metric-types"),s("Put every definition on the tree with a name, a formula, and an ___.",["owner","alert","colour","index"],"owner","An owner is who signs off when the formula changes. Without one, the definition drifts back into people's heads.","canvas-data-product-design")]),d("u2-l2",2,"The logical data model",[a("Five parts","A logical model has **entities** (accounts, deals), **dimensions** (attributes to slice by: region, source), **measures** (aggregates: sum, count), **relationships** (accounts 1:N deals), and **SLOs** (quality conditions). Metrics are measures cut by dimensions.","canvas-data-product-design"),r("Match each example to its model part.",[["accounts, deals","Entities"],["region, source","Dimensions"],["SUM(amount)","Measure"],["accounts 1:N deals","Relationship"]],"Entities are the nouns, dimensions describe them, measures count or sum them, relationships connect them.","canvas-data-product-design"),o('"Conversion rate by region" is…',["A dimension","A measure sliced by a dimension","An entity","An SLO"],1,"The rate is a measure; region is the dimension you cut it by. That's what every metric-tree node looks like.",void 0,"canvas-data-product-design"),n("Dimensions are numeric and measures are text.",!1,"Backwards, and not the real distinction: dimensions are what you slice by, measures are what you aggregate.","canvas-data-product-design"),s("SLOs on the logical model are conditions about data ___ and access.",["quality","volume","colour","storage"],"quality","Freshness, uniqueness, completeness: they live on the model so every consumer inherits the same promises.","canvas-data-product-design"),o("Which belongs in Week 2 (the Canvas)?",["Retiring the legacy dashboard","Building the Metric Dependency Tree and the logical model behind it","Interviewing end users for the first time","Choosing an on-call rotation"],1,"Week 2 turns week 1's definitions into a model everyone can point at, validated with dummy data before production.",void 0,"canvas-data-product-design")]),d("u2-l3",2,"History log vs. current stage",[a("Overwritten fields lose history","The CRM overwrites `deals.stage` on every move. The append-only **deal_stage_history** keeps every transition. Funnel questions are about *ever reached*, so they need the log. Stage-to-stage conversion and time-in-stage don't exist on the current-stage column at all.","funnel-conversion-analysis"),o("How many deals reached *proposal* this year? Which source do you use?",["deals.stage = 'proposal'","deal_stage_history: distinct deals that ever entered 'proposal'","Either; same answer","The CRM's built-in report"],1,"The current-stage column only shows deals sitting in proposal *now*. Deals that moved on are invisible to it.",["Undercounts badly: deals that moved on don't appear.",void 0,"A snapshot and a path are not the same thing.","You can't inspect its definition."],"funnel-conversion-analysis"),r("Match the question to the query shape.",[["Did it close?","won ÷ resolved, by created cohort"],["Where do they drop?","distinct deals per stage from the history log"],["Where do they stall?","LEAD(entered_at) gaps per deal"]],"Three funnel questions, three queries. Keeping them apart is the whole skill.","funnel-conversion-analysis"),n("A deal that skipped the proposal stage will still appear in the proposal count when counting from the history log.",!1,"It never entered proposal, so it has no proposal row. That's correct behaviour, and it's why history-based funnels are honest.","funnel-conversion-analysis"),s("Time in stage = gap to the deal's ___ history row.",["next","first","last","largest"],"next","LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at) minus entered_at. Terminal stages get NULL, which is right.","funnel-conversion-analysis"),o("Reps sometimes never move the stage at all. Which source should be authoritative?",["deals.stage, it's what the CRM shows","The history log, with deals.stage derived from its latest row and a consistency check","Whichever is more recent","A weekly manual reconciliation"],1,"Make the log the source of truth in the transform and add an SLO that flags disagreement. Manual fixes guarantee next month's four.",void 0,"data-quality-dimensions")]),d("u2-l4",2,"Join fan-out",[a("The silent multiplier","Joining a 1:N table *before* an aggregate multiplies the measure once per child row. A deal with five history rows counts its amount five times. Aggregate the N side first, in a CTE, then join. This is the most common way a total gets silently inflated.","sql-toolkit-for-data-pms"),o("```sql\nSELECT a.account_name, SUM(d.amount)\nFROM accounts a\nJOIN deals d ON d.account_id = a.account_id\nJOIN deal_stage_history h ON h.deal_id = d.deal_id\nWHERE d.stage = 'closed_won'\nGROUP BY a.account_name;\n```\nWhat's wrong?",["Nothing","Each deal's amount is counted once per history row: totals are inflated","GROUP BY must include d.amount","It needs a LEFT JOIN"],1,"The WHERE filters deals but the join still repeats each deal per history row. Drop the join or aggregate history separately first.",void 0,"sql-toolkit-for-data-pms"),n("A WHERE clause protects you from join fan-out.",!1,"WHERE filters rows; it doesn't un-multiply them. The fix is where you aggregate, not what you filter.","sql-toolkit-for-data-pms"),s("To avoid fan-out, aggregate the ___ side first.",["N (many)","1 (one)","left","smaller"],"N (many)","Collapse the many side to one row per key in a CTE, then join it to the one side.","sql-toolkit-for-data-pms"),i("Order the safe pattern.",["CTE: aggregate history to one row per deal","Join the CTE to deals","Aggregate deals to one row per account"],"Grain by grain: many → one → one. Each step has a known row count.","sql-toolkit-for-data-pms"),o("Total won value roughly quintupled after someone added a join. Most likely cause?",["A new customer","Fan-out from a 1:N join before the SUM","Currency conversion","The WHERE clause was removed"],1,"A multiple that matches the average child-row count is the fingerprint of fan-out.",void 0,"sql-toolkit-for-data-pms")]),d("u2-l5",2,"Windows vs. GROUP BY",[a("Keep the rows, add the context","**GROUP BY** collapses rows into groups. A **window function** computes over a partition but keeps every row: `ROW_NUMBER()` for latest-per-entity, `LAG`/`LEAD` for change between rows, `SUM() OVER (ORDER BY …)` for running totals. Reach for a window when you need an aggregate *alongside* each row.","sql-toolkit-for-data-pms"),o("When do you use a window function instead of GROUP BY?",["When you want fewer rows out than in","When you need an aggregate or rank alongside each row without collapsing rows","Only for performance","Never in SQLite"],1,"Latest stage per deal, running total by month, change vs previous month: all per-row context.",void 0,"sql-toolkit-for-data-pms"),r("Match the need to the window function.",[["Latest history row per deal","ROW_NUMBER() OVER (PARTITION BY deal_id ORDER BY entered_at DESC)"],["Change vs previous month","LAG(views) OVER (ORDER BY month)"],["Running revenue total","SUM(revenue) OVER (ORDER BY month)"],["Days until next stage","LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at)"]],"Four patterns cover most of what a Data PM writes with windows.","sql-toolkit-for-data-pms"),n("SQLite does not support window functions.",!1,"It has since version 3.25 (2018). The in-browser SQLite here is far newer than that.","sql-toolkit-for-data-pms"),s("`ROW_NUMBER() OVER (PARTITION BY deal_id ORDER BY entered_at DESC)` = 1 gives the ___ row per deal.",["latest","earliest","largest","duplicate"],"latest","DESC puts the newest first; rn = 1 picks it. Add a tiebreaker (history_id DESC) when timestamps can tie.","sql-toolkit-for-data-pms"),o("```sql\nSELECT deal_id, stage,\n  julianday(LEAD(entered_at) OVER (PARTITION BY deal_id ORDER BY entered_at))\n  - julianday(entered_at) AS days\nFROM deal_stage_history;\n```\nWhat is `days` for each deal's most recent stage?",["0","NULL, there is no next row","Days since the deal was created","An error"],1,"LEAD has nothing to look at, so NULL. Right for closed stages; for open deals you may COALESCE with the as-of date.",void 0,"sql-toolkit-for-data-pms")])],c=[d("u3-l1",3,"Four pieces of activation",[a("What makes a data product live","A data product activates when four pieces exist: **input ports** (where data enters), **transformation steps**, **output ports** (where consumers read, usually a Gold table), and **SLOs** (quality promises). The first three ship the number. The fourth is where trust lives.","data-product-activation"),r("Match the piece to its role.",[["Input port","Where data enters from source systems"],["Transformation","Turns raw input into measures and dimensions"],["Output port","Where consumers read the result"],["SLO","Quality and governance conditions"]],"Ports in, transforms, ports out, promises. Miss the promises and you've shipped a report.","data-product-activation"),s("Without a self-service layer, four pieces become ___ of pieces.",["hundreds","two","a handful","dozens"],"hundreds","Credentials, workflows, connectors, monitors, all from scratch. Self-service lets an engineer declare inputs, outputs, and steps in one spec.","self-service-infrastructure"),n("A green pipeline run guarantees the output data is correct.",!1,"Success means the job ran. Duplicates, partial files, and stale upstream data all pass a status check.","data-quality-dimensions"),o("Which piece is most often missing when a data product loses trust?",["Input ports","Transformations","Output ports","SLOs"],3,"Teams build the pipeline and skip the promises. Then nobody can say what the data guarantees, so nobody trusts it.",void 0,"data-product-activation"),o("Where does a data product's output port usually read from?",["Bronze: raw ingestion","Silver: cleaned and joined","Gold: business-level aggregates","The source CRM directly"],2,"Gold is the business-ready layer that feeds dashboards, AI, and reporting. Silver is where most transform bugs live.",void 0,"medallion-architecture")]),d("u3-l2",3,"Six data-quality dimensions",[a("The vocabulary of trust","**Completeness** (required values present), **uniqueness** (each event once), **timeliness** (fresh enough), **consistency** (two representations agree), **validity** (values conform to rules), **accuracy** (matches reality). Each becomes an SLO once you name a measurement, a threshold, and a check time.","data-quality-dimensions"),r("Match the problem to the dimension.",[["Closed deal with NULL closed_date","Completeness"],["Same transaction ingested twice","Uniqueness"],["CRM stage ≠ latest history row","Consistency"],["Latest load is 3 days old","Timeliness"]],"Name the dimension and the SLO writes itself.","data-quality-dimensions"),o("`stage = 'closed-won'` (with a hyphen) appears in 12 rows. Which dimension?",["Accuracy","Validity","Timeliness","Uniqueness"],1,"Validity: the value doesn't conform to the allowed set. Accuracy would be a valid value that's wrong about the world.",void 0,"data-quality-dimensions"),n("A NULL closed_date is an accuracy problem.",!1,"NULL isn't inaccurate, it's absent. That's completeness.","data-quality-dimensions"),s("Uniqueness SLO: no two transactions share (deal, amount, ___).",["date","owner","region","type"],"date","The duplicate key is the set of columns that identifies one real-world event.","data-quality-dimensions"),i("Order the steps of turning a dimension into an SLO.",["Pick the dimension (e.g. timeliness)","Write the measurement as a query","Set the threshold","Decide when it's evaluated"],"Dimension → query → threshold → schedule. Skip the query and it's a wish.","data-quality-dimensions")]),d("u3-l3",3,"Writing an SLO that can be checked",[a("Measurement, threshold, time",'"The pipeline runs at 02:00" is a schedule. "The latest successful load of deals is never more than 24 hours old at 09:00, measured from pipeline_runs" is an SLO. The difference is a query you can run and a number you can breach.',"data-quality-dimensions"),o("Which is a real freshness SLO?",['"Data is always up to date"','"The job runs every day at 02:00"','"Latest non-failed deals load < 24h old at 09:00, from pipeline_runs"','"Raj is responsible for freshness"'],2,"Measurement, threshold, and evaluation time. The others are a slogan, a schedule, and an org chart.",void 0,"data-quality-dimensions"),s("An SLO has three parts: a measurement, a ___, and when it is evaluated.",["threshold","dashboard","budget","vendor"],"threshold","Without a threshold there's nothing to breach, so nothing to alert on.","data-quality-dimensions"),n('"100% of closed deals have a closed_date" is a completeness SLO.',!0,"Required field, required population, explicit threshold. It's checkable with one query.","data-quality-dimensions"),o("A freshness check found the gold table 3 days stale over a weekend. What should have existed?",["A bigger warehouse","An SLO with a monitor that alerts on breach, and a time-to-recover metric for on-call","A Monday morning meeting","A retry button"],1,"Three days with no alert is the failure. Time to recover is what an on-call rotation is measured on.",void 0,"data-product-activation"),r("Match the SLO to what it protects against.",[["Uniqueness on transactions","Re-ingested file double-counting revenue"],["Row-count vs trailing average","Partial file reported as success"],["Consistency stage vs history","Won deals missing from bookings"],["Freshness < 24h","Stale numbers in the Monday deck"]],"Every planted problem in Meridian's data maps to one SLO.","data-quality-dimensions")]),d("u3-l4",3,"Success is not correct",[a("What a status column can't see","Revenue jumped +189,500 overnight and fell back; every run was green. A re-ingested file (**duplicates**) passes every status check. A **partial file** (30% of rows) passes too. Volume and uniqueness checks catch what monitoring never will.","data-quality-dimensions"),o("Revenue tile jumps by exactly one deal's amount, then reverts next morning. First check?",["Dashboard cache","Duplicate transactions (same deal, amount, date)","Dana edited the definition","A new deal"],1,"A jump matching a deal amount that reverts is the signature of a re-ingested file.",["A cache doesn't invent a deal-sized number.",void 0,"Definitions don't change overnight and revert.","A new deal wouldn't disappear."],"data-quality-dimensions"),s("The duplicate idiom: `GROUP BY key HAVING COUNT(*) > ___`.",["1","0","2","n"],"1","Any group with more than one row is a duplicate of the key. It's a one-liner that becomes a nightly SLO.","sql-toolkit-for-data-pms"),n("A run that processed 30% of the usual rows will show status = 'failed'.",!1,"It succeeded on the rows it got. Only a row-count check against a trailing average flags it.","data-quality-dimensions"),o("Which window frame gives a per-run baseline for a volume check?",["ROWS BETWEEN 7 PRECEDING AND 1 PRECEDING","ROWS BETWEEN CURRENT ROW AND 7 FOLLOWING","GROUP BY run_date","ORDER BY rows_out DESC"],0,"The previous seven runs, excluding the current one, is the trailing average to compare against.",void 0,"sql-toolkit-for-data-pms"),o('Raj says "the numbers are right, every run succeeded." The Data PM\'s reframe?',['"Then the dashboard is wrong"','"Here is what the runs can\'t guarantee, and here are the checks that would"','"Let\'s rebuild the pipeline"','"Let\'s ask Dana to trust it"'],1,"Pipeline observability (did it run?) and data quality (is it right?) are different questions. Naming the gap turns a defensive engineer into an owner of the checks.",void 0,"four-key-shifts")]),d("u3-l5",3,"NULL and the LEFT JOIN trap",[a("How completeness checks return nothing","`NULL = 'x'` is never true. A LEFT JOIN with a WHERE condition on the right-hand table turns into an inner join, dropping exactly the missing rows you were looking for. Put the condition in `ON`, and always write `IS NULL`, never `= NULL`.","sql-toolkit-for-data-pms"),o("```sql\nSELECT d.deal_id, t.transaction_id\nFROM deals d\nLEFT JOIN transactions t ON t.deal_id = d.deal_id\nWHERE d.stage = 'closed_won' AND t.type = 'initial';\n```\nWhy won't this find won deals with no transaction?",["LEFT should be RIGHT","The WHERE on t.type drops rows where t is NULL, making it an inner join","It needs DISTINCT","It errors on NULL"],1,"Move `t.type = 'initial'` into the ON clause, or allow `t.type IS NULL`.",void 0,"sql-toolkit-for-data-pms"),s("To find closed deals missing a date: `WHERE closed_date ___`.",["IS NULL","= NULL","== NULL","IS EMPTY"],"IS NULL","NULL compares as unknown with everything, including NULL. IS NULL is the only test that works.","sql-toolkit-for-data-pms"),n("Conditions on the right-hand table of a LEFT JOIN belong in the ON clause.",!0,"In ON they filter the match; in WHERE they filter the result and discard the unmatched rows.","sql-toolkit-for-data-pms"),o("A completeness check returns zero rows on the first run. Most likely?",["The data is perfect","The query silently filtered out the missing rows","The table is empty","The SLO threshold is wrong"],1,"Zero problems on a first audit is a red flag for the query, not a green light for the data.",void 0,"data-quality-dimensions"),r("Match the bug to its symptom.",[["LEFT JOIN + WHERE on right table","Missing rows vanish from the check"],["= NULL","Condition is never true"],["COUNT(*) vs COUNT(DISTINCT)","People counted as views"],["Integer division","A rate of 0"]],"Four silent bugs. None throws an error; all corrupt a metric.","sql-toolkit-for-data-pms")])],h=[d("u4-l1",4,"Adoption is the metric",[a("A product nobody uses is a report","Launch day is when the definitions you fought for become the definitions people decide with, or don't. Adoption isn't vanity for a data product; it's the only evidence the product exists. Measure **distinct people per week, by role**, and compare against whatever they used before.","go-to-market-launch"),o('Sofia asks "Is anyone using it?" Which number do you bring?',["Total page views","Weekly distinct viewers by role, next to the same for the legacy report","Number of dashboards built","A satisfaction survey"],1,"Distinct people tell you who adopted; role tells you who's missing; the legacy comparison tells you whether you replaced anything.",["Two analysts refreshing all day inflate this.",void 0,"Output, not outcome.","Useful later; doesn't answer the question."],"go-to-market-launch"),n("Raw view counts are a good adoption metric.",!1,"Views measure activity. Adoption is people: COUNT(DISTINCT viewer_id).","go-to-market-launch"),s("Report adoption as distinct viewers per ___, not raw views.",["role","hour","browser","chart"],"role","A role with zero viewers (Finance at Meridian) is next week's stakeholder conversation.","go-to-market-launch"),o("Which Playbook week gets the *highest* share of effort?",["Week 1 discovery","Week 2 modeling","Week 3 activation","Weeks 5–6 post-launch and adoption"],3,"Post-launch is where you learn how real users behave and where the feedback loop that drives the MVP comes from.",void 0,"proof-of-value-performance"),i("Order a launch week.",["Position it in the users' language","Make usage native (in the tools they already use)","Measure distinct viewers by role","Decide: train, embed, or retire the old report"],"Positioning, access, measurement, decision. The decision is the point of measuring.","go-to-market-launch")]),d("u4-l2",4,"Exist where the user already is",[a("Native accessibility","Reps won't open a new dashboard because it's there. Surface the stalled-deal metric inside the **CRM record** and the manager's **1:1 view**, where the decision already happens. Work with what users work with; adapt when they bring new tools.","go-to-market-launch"),o('For Meridian\'s reps, "exist where the user is" most likely means…',["A weekly PDF","The stalled-deal metric inside the CRM record and the 1:1 view","A mobile app","Mandatory dashboard login"],1,"Show up in the tool and the ritual where the decision is made. New surfaces create new adoption problems.",void 0,"go-to-market-launch"),n("Sales Managers adopted first at Meridian, and reps followed once managers asked about stalled deals in 1:1s.",!0,"Adoption spreads through the ritual, not the announcement.","go-to-market-launch"),r("Match the launch tactic to its purpose.",[["Rich semantics in the model","Speak the users' language"],["Templates and samples repo","Accessible documentation"],["Standard downstream-update notice","Consumers trust changes"],["Usage metrics per asset","Adoption booster and evidence"]],"The Playbook's launch checklist, one line each.","go-to-market-launch"),s("Use-case-specific insights to share at launch: boosting a metric, running ___ on a metric, spotting low-yield initiatives.",["RCA","ads","surveys","backups"],"RCA","Show people how to use the metric tree for the three things they actually do with numbers.","go-to-market-launch"),o("Finance has zero viewers three weeks in. What is that?",["A failed launch","A stakeholder conversation to have with Tomás next week","A reason to email everyone","Proof finance doesn't need data"],1,'Adoption by role turns a vague "nobody uses it" into a named person and a next step.',void 0,"go-to-market-launch")]),d("u4-l3",4,"Views vs. viewers",[a("One word, one bug","`COUNT(*)` counts rows (views). `COUNT(DISTINCT viewer_id)` counts people (viewers). \"Active viewers\" computed with COUNT(*) is inflated by anyone who refreshes. Bucket weeks with `strftime('%Y-%W', viewed_at)`; aggregate to the grain you report.","sql-toolkit-for-data-pms"),o("```sql\nSELECT strftime('%Y-%W', viewed_at) AS week, COUNT(*) AS active_viewers\nFROM dashboard_views GROUP BY week;\n```\nWhat does `active_viewers` really count?",["Distinct people per week","Views per week; you need COUNT(DISTINCT viewer_id)","Weeks with a view","It errors"],1,"Rows, not people. Someone with 20 views counts 20 times.",void 0,"sql-toolkit-for-data-pms"),s("Views per viewer = COUNT(*) ÷ COUNT(DISTINCT ___).",["viewer_id","dashboard","week","role"],"viewer_id","Both aggregates in one pass; multiply by 1.0 first if your engine does integer division.","sql-toolkit-for-data-pms"),n("`strftime('%Y-%W', ts)` gives a week bucket in SQLite.",!0,"%W is the week of the year (Monday-based). substr(date,1,7) does the same for months.","sql-toolkit-for-data-pms"),o("Two RevOps analysts refresh the dashboard all day. Which metric stays honest?",["Total views","Distinct viewers by role","Views per day","Average session length"],1,"They count as two people in one role. Every view-based number is distorted.",void 0,"go-to-market-launch"),r("Match the grain to the bucket expression.",[["Month","substr(viewed_at, 1, 7)"],["Week","strftime('%Y-%W', viewed_at)"],["Day","substr(viewed_at, 1, 10)"]],"Pick the grain your audience reads at, then aggregate before any window.","sql-toolkit-for-data-pms")]),d("u4-l4",4,"Retire the old report",[a("Two numbers for one thing is the problem you were hired to fix","Three weeks after launch the legacy report still gets a trickle of views. Don't leave it (two truths), don't delete it (broken bookmarks). **Redirect** the old URL to the new product with a banner about the definition change, and talk to the remaining regulars first.","go-to-market-launch"),o("Legacy report still has a dozen regular viewers. What do you do?",["Leave it","Delete it now","Redirect with a banner, after talking to the regulars","Email a reminder"],2,"The redirect meets people at their bookmark; the conversation catches any real need the new product misses.",["Two numbers for the same thing erodes trust.","Breaks bookmarks and goodwill.",void 0,"Emails don't change bookmarks."],"go-to-market-launch"),n("Keeping the legacy report around 'for people who prefer it' is harmless.",!1,"It preserves the second definition, so every future disagreement can cite it.","go-to-market-launch"),s("Retire the legacy report with a ___, not a memo.",["redirect","meeting","survey","vote"],"redirect","A redirect changes behaviour at the moment of use; a memo asks people to remember.","go-to-market-launch"),o("Which SQL shows whether the new dashboard replaced the old one?",["Total views since launch","Monthly views per dashboard with LAG for the change vs previous month","Distinct dashboards","Max views in a day"],1,"Legacy fading while the new one grows, month over month, is the evidence for the redirect decision.",void 0,"sql-toolkit-for-data-pms"),i("Order the retirement.",["Confirm the new product covers the regulars' needs","Announce the definition change on the new dashboard","Redirect the legacy URL","Watch the legacy views go to zero"],"Check, explain, redirect, verify. Deleting first skips the two steps that keep trust.","go-to-market-launch")]),d("u4-l5",4,"Where a metric lives",[a("Bronze, Silver, Gold","**Bronze** is raw ingestion, **Silver** is cleaned and joined, **Gold** is business-level aggregates that dashboards and AI read. It's the shared vocabulary with engineers for *where* a number is made, and for root cause: a broken metric is usually a Silver transform, not a Gold aggregation.","medallion-architecture"),r("Match the layer to what it holds.",[["Bronze","Raw ingestion, as it arrived"],["Silver","Filtered, cleaned, joined with reference data"],["Gold","Business-level aggregates for reporting and AI"]],"Three hops from source to decision.","medallion-architecture"),o("A metric is wrong because two customer records were joined on the wrong key. Which layer?",["Bronze","Silver","Gold","The dashboard"],1,"Joins and cleaning happen in Silver. Gold just aggregates whatever Silver gives it.",void 0,"medallion-architecture"),n("Dashboards should read from Bronze so they always have the latest raw data.",!1,"Bronze is untyped, undeduplicated, and unjoined. Output ports read from Gold.","medallion-architecture"),s("The self-service layer lets an analytics engineer declare inputs, outputs, and transform steps in a single ___ file.",["spec","PDF","Excel","email"],"spec","Declarative specs (often YAML) turn hundreds of plumbing pieces into one document the platform expands.","self-service-infrastructure"),o("The OEE manufacturing case computed availability × efficiency × quality in near real time. Which layer held the aggregated OEE for dashboards?",["Bronze","Silver","Gold","Kafka"],2,"Sensor data landed in Bronze, was cleaned and joined with ERP data in Silver, and aggregated to OEE in Gold.",void 0,"oee-manufacturing-case-study")])],m=[d("u5-l1",5,"Is the drop real?",[a("Rule out the artifact first",'Conversion for May–June looks low. Before any theory: restrict to **resolved** deals (won + lost) on cohorts old enough to have closed. "Won ÷ created" always sags at the right edge because recent deals haven\'t had time. Only then slice.',"funnel-conversion-analysis"),o("Conversion for recent cohorts looks low. First thing to rule out?",["The price increase","The open-deal artifact: recent cohorts haven't had time to close","Dana reading the chart wrong","Reps lying in the CRM"],1,"Confirm the number is real before assigning a cause. Filter to resolved deals and mature cohorts.",["A hypothesis to test, not an artifact to exclude.",void 0,"Check the data before the reader.","A specific hypothesis, not the first artifact."],"funnel-conversion-analysis"),s("Compute conversion on ___ deals only: won ÷ (won + lost).",["resolved","all","open","enterprise"],"resolved","Open deals have neither converted nor failed. Including them turns a rate into a countdown.","funnel-conversion-analysis"),n("If the drop survives the resolved-deals filter, it's real enough to investigate.",!0,"That's step one of root cause: prove the number, then walk down the tree.","funnel-conversion-analysis"),o("Which cohort filter is right for a drop in May–June, as of end of August?",["created_date <= '2026-06-30'","created_date >= '2026-08-01'","closed_date IS NULL","No filter"],0,"Cohorts through June have had two months to resolve. July and August haven't, so they'd contaminate the picture.",void 0,"funnel-conversion-analysis"),i("Order the root-cause walk.",["Confirm the drop on resolved, mature cohorts","Slice by one dimension at a time","Zoom into the affected cell: stage and velocity","Size it and propose a fix with an owner"],"Confirm → slice → zoom → size. Arguing at the top of the tree skips all four.","funnel-conversion-analysis")]),d("u5-l2",5,"Walk down the tree",[a("A cause that touches everything can't explain one cell","Slice the drop by region × source. If AMER × Outbound collapses and every other cell is flat, a **price increase** (which hits every region and source) is refuted in one query. Lena's **vendor change for AMER outbound leads** predicts exactly that cell.","funnel-conversion-analysis"),o("The drop is almost entirely AMER × Outbound; other cells are flat. Dana's price-increase theory is…",["Supported, AMER is biggest","Mostly refuted: a price increase would depress every segment","Untestable without pricing data","Confirmed, outbound is price-sensitive"],1,"A universal cause can't produce a local effect. The tree kills theories cheaply.",["Size doesn't matter; pattern does.",void 0,"The shape of the change is already strong evidence.","That's inventing a mechanism to save a theory."],"funnel-conversion-analysis"),s("Slice by ___ dimension at a time, then zoom into the cell that moved.",["one","every","the largest","a random"],"one","One dimension per cut keeps the comparison readable and the cause attributable.","funnel-conversion-analysis"),n("Raj's stale-stage theory could explain a whole-segment collapse.",!1,"It explained a handful of deals. A dozen missing stage updates can't move a segment's rate from 0.58 to 0.07.","funnel-conversion-analysis"),r("Match the theory to the evidence that tests it.",[["Price increase","Every segment should drop"],["Vendor lead quality","Only AMER × Outbound drops"],["Stale CRM stages","Only a few deals disagree with history"],["Count vs value definition","Both definitions show the same shape"]],"Each theory makes a prediction. Write the prediction down, then run the query.","funnel-conversion-analysis"),o("Which SQL shape produces the before/during comparison for every region × source cell?",["Two separate queries per region","Conditional aggregation: SUM(period='before' AND won) / SUM(period='before'), same for during, GROUP BY region, source","A cross join","A window over deal_id"],1,"One query, nine cells, two periods. Conditional aggregation is the tool for side-by-side rates.",void 0,"sql-toolkit-for-data-pms")]),d("u5-l3",5,"Where in the funnel",[a("Late losses point at lead quality","In the affected segment, deals still reach negotiation at the normal rate but spend twice as long there and then close lost. Losses **late** in the funnel with longer negotiation usually mean the leads looked qualified but weren't. Losses **early** mean targeting.","funnel-conversion-analysis"),o("Deals reach negotiation normally, stall twice as long, then close lost. Best explanation?",["Reps not updating stages","Lead quality dropped: qualified on paper, fail at the table","A pipeline failure dropping won deals","The CFO's definition"],1,"A top-of-funnel quality problem shows up late as long negotiation and losses. It matches the vendor-change hypothesis.",["Staleness looks like stuck deals with no later row, not eventual losses.",void 0,"That would cut won counts without lengthening negotiation.","Definitions don't change stage velocity."],"funnel-conversion-analysis"),n("A stage whose duration doubles is a stronger RCA signal than a rate that wobbles.",!0,"Velocity changes are hard to produce by noise; rates on small cells wobble constantly.","funnel-conversion-analysis"),s("Late-funnel losses with longer negotiation usually mean ___ quality.",["lead","data","code","pricing"],"lead","They got to the table and couldn't close: the qualification was wrong, not the sales motion.","funnel-conversion-analysis"),o("Which query compares negotiation time before vs during for one segment?",["GROUP BY owner","LEAD-based days-in-stage CTE, joined to a segment CTE with a period label, AVG(days) GROUP BY period","COUNT(*) per stage","A running total"],1,"Three CTEs, each named for what it produces: history steps, the segment with periods, then the average per period.",void 0,"sql-toolkit-for-data-pms"),r("Match the funnel position of the loss to the likely cause.",[["Early (prospecting → qualified)","Targeting / wrong ICP"],["Middle (proposal)","Offer or pricing fit"],["Late (negotiation, long)","Lead quality or decision-maker access"]],"Where deals die tells you what to fix.","funnel-conversion-analysis")]),d("u5-l4",5,"LAG and LEAD",[a("Looking at the neighbouring row","`LAG(x) OVER (ORDER BY …)` reads the previous row, `LEAD(x)` the next. Partition by the entity (deal, dashboard) so you never look across entities. First and last rows get NULL: expected, and sometimes the thing you're looking for.","sql-toolkit-for-data-pms"),s("Change vs previous month = views − ___(views) OVER (PARTITION BY dashboard ORDER BY month).",["LAG","LEAD","SUM","MAX"],"LAG","LAG looks back one row within the dashboard's ordered months.","sql-toolkit-for-data-pms"),o("Why partition by deal_id when computing time between stages?",["For performance","So LEAD never reads another deal's row","SQLite requires it","To sort the output"],1,"Without the partition, the last row of one deal would look at the first row of the next deal and produce a nonsense gap.",void 0,"sql-toolkit-for-data-pms"),n("LEAD returns 0 for the last row in a partition.",!1,"It returns NULL (or the default you pass). Filter it out for terminal stages, or COALESCE with the as-of date for open deals.","sql-toolkit-for-data-pms"),r("Match the function to its direction.",[["LAG","Previous row"],["LEAD","Next row"],["ROW_NUMBER","Position within the partition"],["SUM OVER ORDER BY","Cumulative so far"]],"Four window verbs. Everything else is a variation.","sql-toolkit-for-data-pms"),o("The first month of each dashboard shows NULL for change_vs_prev. Is that a bug?",["Yes, use 0","No, there is no previous month; NULL is honest","Yes, drop the row","Yes, use the average"],1,'NULL says "no comparison exists." Replacing it with 0 would claim the dashboard had zero growth in its first month.',void 0,"sql-toolkit-for-data-pms")]),d("u5-l5",5,"Bring a cause, a size, and a fix",[a("RCA ends with a claim","Root-cause work is done when someone can act: a **cause**, its **size** (deals and dollars), the **evidence that rules out alternatives**, and a **fix with an owner**. A chart and four theories is where you started.","proof-of-value-performance"),o("You've localized the drop. What goes to exec staff?",["The chart, let them debate","Cause, size in deals and dollars, evidence ruling out alternatives, proposed fix with an owner","Fire the vendor","Ask for more data"],1,"Sizing lets Sofia weigh the fix; ruling out alternatives is what earns trust; an owner makes it happen.",["Back to four theories.",void 0,"Too far, too fast. Propose reverting the scoring model; let Lena decide on the vendor.","Evidence is concentrated and alternatives refuted. Delay costs money."],"proof-of-value-performance"),i("Order the RCA deliverable.",["The cause, in one sentence","How big it is (deals, dollars)","What it is not (alternatives ruled out)","The fix, its owner, and when"],"Claim, size, evidence, action. Executives read in that order.","proof-of-value-performance"),s("Size the problem in deals and ___.",["dollars","rows","queries","meetings"],"dollars","Lost value by rep for the segment and period is the slide's one number.","proof-of-value-performance"),n('Recommending "fire the vendor" is the right conclusion of the AMER × Outbound analysis.',!1,"The evidence supports reverting the scoring model and re-qualifying open deals. The vendor decision belongs to Lena, with more data.","proof-of-value-performance"),o("What turns a proof of value into a lasting product after week 5?",["A bigger dashboard","Feedback loops that connect outcomes to activity, plus evolving SLOs and use cases","More pipelines","A press release"],1,"The Playbook's post-launch stage: continuous feedback, usage metrics, SLO evolution, use-case expansion.",void 0,"proof-of-value-performance")])],p=[d("u6-l1",6,"Bookings vs. revenue",[a("Neither number is wrong","Board deck: 7.77M (sum of **transactions**: cash, incl. renewals and upsells). Sales deck: 6.84M (sum of **closed_won amounts**: bookings). They diverge where the business is healthy. Give both a name and an owner, and show the **bridge** between them.","ontology-as-infrastructure"),o("Tomás asks which revenue number is right. Your answer?",["Transactions, cash is truth","Neither is wrong: two metrics (bookings vs revenue); name both, show the bridge, put both on the slide","Average them","The higher one"],1,"The same word meant two things. The deliverable is names, owners, and a reconciliation, not a winner.",["Cash is one truth; bookings is the truth Sales is managed on.",void 0,"Nobody can reproduce an average of two definitions.","That's how trust is lost with a CFO."],"ontology-as-infrastructure"),r("Match the bucket to its role in the bridge.",[["initial transactions","Should reconcile to bookings"],["renewals + upsells","The healthy part of the gap"],["duplicates","The part you remove"]],"Every dollar of the difference gets a name.","ontology-as-infrastructure"),s("Bookings come from deals; revenue comes from ___.",["transactions","accounts","dashboards","pipeline_runs"],"transactions","Different tables, different owners (Sales vs Finance), different meanings.","metric-types"),n("A UNION of months from both sides, then LEFT JOIN each side, compares two metrics that should agree without losing months present in only one.",!0,"That's the reconciliation pattern: build the key set first, then attach both sides with COALESCE.","sql-toolkit-for-data-pms"),o("Which name pair would you put on the board slide?",["revenue and revenue","bookings_value (Sales, from deals) and revenue_generated (Finance, from transactions)","sales and money","gross and net"],1,"Two names, two owners, two tables. The bridge explains the difference line by line.",void 0,"ontology-as-infrastructure")]),d("u6-l2",6,"Ontology as infrastructure",[a("Humans resolved ambiguity silently; agents can't","For years, every system kept its own definition of *customer* or *revenue* and people reconciled in conversation. An AI agent takes the first definition it finds and acts on it. A written **ontology** (what a word means, how concepts relate, who owns them) is now operational infrastructure, not documentation.","ontology-as-infrastructure"),o("Why did enterprises manage without formal ontologies for so long?",["Data was small","Humans silently resolved the ambiguity in every conversation","Regulators didn't require them","They had them, just unwritten"],1,"Meaning lived in silos and in people's heads. Autonomous agents remove the human who used to reconcile.",void 0,"ontology-as-infrastructure"),n("In the Q2 2026 survey, leaders ranked a semantic layer with standard definitions above AI tooling itself as the top enabler of AI.",!0,"80% put the semantic layer first. Definitions before models.","ontology-as-infrastructure"),s("A data product is built once and ___ consume it forever.",["agents","auditors","vendors","interns"],"agents","Instead of rebuilding context from scratch on every query, an agent reads the data product's contract.","ontology-as-infrastructure"),r("Match the ambiguity to the question an ontology answers.",[['"revenue"',"GAAP-recognized or bookings?"],['"customer"',"CRM record or billing record?"],['"approved"',"By whom, in which system?"]],"Every one of these is a decision a human used to make without noticing.","ontology-as-infrastructure"),o("What's the sharpest question for a data leader in 2026?",["Are we using AI?","What does our AI think our business means, and is that what we actually mean?","How many agents do we have?","Which model is cheapest?"],1,"Usage is easy. Meaning is the gap between agents that run and agents that are right.",void 0,"ontology-as-infrastructure")]),d("u6-l3",6,"Observability is not evaluation",[a("It ran vs. it was right","Most teams running agents have **monitoring** (latency, tokens, error rate). About half have **evaluation** (known questions, agreed answers, run before and after every change). Monitoring tells you the agent ran. Only evaluation tells you it reasoned correctly.","agent-observability-gap"),o("The agent team has full observability and wants to answer executives' revenue questions. What do you ask for first?",["A faster model","An evaluation set from the semantic contract, run on every change","More dashboards","Block the agent"],1,"Without evals you're watching failure at machine speed. With them, the ontology becomes testable.",["Speed isn't the risk.",void 0,"Dashboards report what happened; they don't judge correctness.","Unnecessary once it's gated by definitions and evals."],"agent-observability-gap"),n("Normal software tests (same input → same output) work fine for agents.",!1,"Agents are non-deterministic: many valid paths to a task, and a plausible output can come from broken reasoning.","agent-observability-gap"),r("Match the primitive to what it catches.",[["Trace IDs end to end","Where a multi-step thread went wrong"],["Per-tool-call token budgets","Runaway cost in one step"],["Semantic assertion layer","Wrong answers before they reach a user"]],"The three observability primitives most production teams are missing.","agent-observability-gap"),s("Monitoring says it ran; ___ says it was right.",["evaluation","logging","caching","scaling"],"evaluation","Build the eval set from the same definitions people use, so agent and human are graded on one standard.","agent-observability-gap"),o("Where does the effort belong, according to the 2026 reckoning?",["More dashboards after the fact","Upstream: semantic infrastructure, data contracts, and governance that define correct reasoning before the agent runs","Bigger models","Fewer agents"],1,"Define correct before you run, not after. Dashboards can only report what already happened.",void 0,"agent-observability-gap")]),d("u6-l4",6,"Lean AI",[a("Match effort to difficulty","Per-token pricing is fine at demo scale and terrible at real scale. Small task-specific models can be ~100× cheaper per conversation. Mature teams **route by difficulty**: narrow models for the high-volume well-defined slice, frontier models only for the hard fraction. Report **cost per correct answer**.","lean-ai-cost-economics"),o("Finance asks why the agent pilot's bill is so high. The Lean AI move?",["Smallest model for everything","Route by difficulty; report cost per correct answer","Cap questions per user","Cache answers"],1,"Cost-per-outcome is what a board reads. Model choice becomes a product-tiering decision.",["Quality collapses on hard questions.",void 0,"Rationing doesn't fix cost per answer.","Helps repeats, not new questions."],"lean-ai-cost-economics"),n('"Small model" still means a quality compromise in 2026.',!1,"Capability decoupled from size. For well-defined tasks, small models match quality at a fraction of the cost.","lean-ai-cost-economics"),s("The Toyota parallel: produce more value with fewer ___, without sacrificing quality.",["resources","people","models","meetings"],"resources","Lean AI is the Toyota Production System applied to models: remove waste, keep quality.","lean-ai-cost-economics"),r("Match the workload to the model tier.",[["Classify 1M support tickets","Narrow / small model"],["Draft a board memo from messy notes","Frontier model"],["Answer 'what is our revenue?' from the contract","Narrow model + semantic layer"]],"Big AI isn't wrong; defaulting to it for tasks that don't need it is the waste.","lean-ai-cost-economics"),o("Which metric should the pilot report monthly?",["Tokens used","Cost per correct answer, by tier","Number of prompts","Model version"],1,"Ties spend to outcomes and makes routing decisions visible.",void 0,"lean-ai-cost-economics")]),d("u6-l5",6,"Grain first, window second",[a("Running totals done right","Decide what one row means (a month), aggregate to that grain in a CTE, *then* apply the window: `SUM(revenue) OVER (ORDER BY month)`. Skip the CTE and the running total steps up on every transaction row. The capstone: North Star, functional, and granular in one query, one definition each.","sql-toolkit-for-data-pms"),o("```sql\nSELECT month, SUM(revenue) OVER (ORDER BY month) AS running_total\nFROM (SELECT substr(transaction_date,1,7) AS month, SUM(amount) AS revenue\n      FROM transactions GROUP BY month);\n```\nWhy aggregate in the subquery first?",["Windows can't run on raw tables","So there is one row per month; otherwise the total steps up per transaction","SUM OVER requires GROUP BY","Performance only"],1,"Grain first, window second. The result would be different, not just slower.",void 0,"sql-toolkit-for-data-pms"),s("Aggregate to the reporting ___ before applying a window.",["grain","index","colour","region"],"grain","One row per month, then cumulate. One row per transaction, then cumulate, is a different (wrong) chart.","sql-toolkit-for-data-pms"),n("A running total over raw transaction rows and one over monthly totals end at the same final number.",!0,"The last value matches; every intermediate point differs. That's why the chart lies while the total looks fine.","sql-toolkit-for-data-pms"),i("Order the capstone query.",["CTE rev: revenue by transaction month","CTE closed: won value and conversion by closed month","Join on month, COALESCE missing revenue","Filter to the last three months, order by month"],"Two grains, one join key, one result: the whole metric tree.","metric-types"),o("Six weeks in, what did Meridian actually get?",["Every metric in the company defined","One domain moved from Reactive to Purpose-driven: one metric tree people decide with","A new warehouse","A hiring plan"],1,"That's what a proof of value is. Scale comes from repeating the template, not from boiling the ocean.",void 0,"data-product-maturity-stages")])],g={1:"#4f46e5",2:"#0891b2",3:"#d97706",4:"#16a34a",5:"#dc2626",6:"#7c3aed"},w={1:l,2:u,3:c,4:h,5:m,6:p},f=t.chapters.map(e=>({number:e.number,title:e.title,week:e.week,tagline:e.tagline,color:g[e.number],lessons:w[e.number]})),y=f.flatMap(e=>e.lessons);y.map(e=>[e.id,e]),e.s(["allLessons",0,y,"units",0,f])},47371,e=>{"use strict";let t=[{number:1,slug:"bullseye",title:"Nobody trusts the number",week:"Week 1 · The Bullseye",tagline:"Find the real problem before you touch a query.",brief:`**Monday, 9:05.** Your laptop is still provisioning when Dana Whitfield walks into the room without knocking.

> "The dashboard says our conversion rate is **61%**. It is not 61%. My reps would be swimming in commission. Can you make it right by Friday?"

Ten minutes later Raj Patel messages you: *"Heads up, Dana thinks the numbers are wrong. All three pipelines have been green for months. Happy to walk you through them."* Lena Fischer from RevOps adds you to a channel called **#crm-hygiene** with no explanation.

This is the moment the Four Key Shifts become real. A general PM would ship a fix. A Data PM asks: *which* number, defined *how*, trusted by *whom*, used for *what decision*?

**Your job this week is discovery, not SQL.** Meet the people, map how they use data today, and pin down the North Star, functional, and granular metrics for a Sales domain. The warm-up exercises let you touch the data so you know what exists, but the real deliverable is a problem statement Dana and Sofia both sign.`,readings:["what-is-a-data-product-manager","four-key-shifts","bullseye-data-product-market-fit","metric-types","data-product-maturity-stages"],debrief:`**What a good Week 1 looked like.**

- You did *not* fix the dashboard. You found out that "61%" was won \xf7 closed, that Dana's mental model was won \xf7 created, and that the two definitions differ by 15 points on the same data. Neither was "wrong."
- You wrote a problem statement in Dana's words: *"I can't see where deals stall or which segment is dragging conversion."* Sofia agreed that is the proof-of-value target.
- You picked the metric tree: North Star **revenue_generated**, functional **deals_closed_value**, granular **conversion_rate** and **days_in_stage**.
- You noticed Meridian is in the *Reactive* stage: Raj builds what he's asked; nobody owns definitions.

Next week you turn those definitions into a model everyone can point at.`},{number:2,slug:"canvas",title:"Define it before you compute it",week:"Week 2 · The Canvas",tagline:"Build the metric tree and the logical model behind it.",brief:`**Tuesday, 14:30, a whiteboard.** Three people, three conversion rates.

- **Dana:** won \xf7 (won + lost). "Open deals haven't converted or failed yet."
- **Lena:** won \xf7 created. "That's what the CRM report has always shown."
- **Tom\xe1s:** won *amount* \xf7 closed *amount*. "I don't care how many deals. I care how many dollars."

All three are legitimate. All three will produce a different chart. The Data PM's job is not to pick the winner in the meeting; it is to put every definition on the **Metric Dependency Tree** with a name, a formula, and an owner, so the fight happens once instead of every month.

Raj shows you something else: deal stages are *overwritten* in the CRM, but there is an append-only **deal_stage_history** log. That changes what you can measure. Stage-to-stage conversion, time in stage, funnel drop-off: none of it exists on the \`deals\` table alone.

**This week:** draft the logical model (entities, dimensions, measures, relationships, SLOs) and compute the funnel from the history log. The SQL gets real: CTEs, self-joins on the history, and your first window function.`,readings:["canvas-data-product-design","metric-types","funnel-conversion-analysis","sql-toolkit-for-data-pms"],debrief:"**What a good Week 2 looked like.**\n\n- The MDT now has *three* conversion definitions, each named: `conversion_rate_closed` (Dana), `conversion_rate_created` (Lena), `conversion_rate_value` (Tomás). The dashboard will show Dana's by default and label it.\n- You modeled the funnel from **deal_stage_history**, not from the current stage. Overwritten fields destroy history; append-only logs preserve it. This is the single most important modeling lesson in the chapter.\n- You learned that a stage-to-stage rate (proposal → negotiation) and an overall rate (created → won) answer different questions, and that time-in-stage is where deals actually stall.\n- SLOs made it onto the model for the first time: freshness of `deals` (< 24h), uniqueness of `transaction_id`, and completeness of `closed_date` for closed deals.\n\nNext week you find out whether the data actually meets those SLOs. Spoiler: it does not."},{number:3,slug:"activation",title:"The pipeline says success",week:"Week 3 · Activation",tagline:"Green runs, wrong numbers. Audit the data, not the dashboard.",brief:`**Wednesday, 07:50.** Sofia forwards you a screenshot. The revenue tile on the new prototype jumped **+189,500** overnight, then dropped back the next morning.

Raj is defensive and has a point: every run of \`gold_sales_metrics\` succeeded. Then you look at the run log and find three straight days in June where it didn't, and one morning in July where \`crm_deals_ingest\` processed **30%** of the usual rows and still reported success.

Lena, quietly: *"Reps close deals in the CRM days after the contract is signed. Sometimes they never move the stage at all."*

This is Activation week: input ports, transforms, output ports, and **SLOs**. The first three exist. The fourth is where trust lives. Nobody is going to audit this data for you.

**This week:** write the data-quality checks a Data PM would insist on before launch. Duplicates, stale stages, missing dates, freshness, failed-run streaks, and row-count anomalies. Window functions do the heavy lifting.`,readings:["data-product-activation","data-quality-dimensions","medallion-architecture","self-service-infrastructure"],debrief:'**What a good Week 3 looked like.**\n\n- You found **five duplicate transactions** (a re-ingested file) which explains the +189,500 jump. Uniqueness check added to the pipeline; Raj now owns it.\n- You found **four deals** whose CRM stage still said `negotiation` while the history log said `closed_won`. The source of truth for stage is now the history log, and `deals.stage` is derived from it.\n- You found closed deals with no `closed_date`, a three-day failed streak with no alert, and a partial-file day that passed as "success." Each became an SLO with a monitor.\n- Raj went from "the numbers are right" to "here is what the numbers can\'t guarantee." That is the team dynamic shift: *how do we validate?*\n\nNext week you ship, and adoption becomes the metric.'},{number:4,slug:"launch",title:"Ship it where they already are",week:"Week 4 · Launch",tagline:"A data product with no adoption plan is a report.",brief:`**Monday, 1 June.** The Sales Funnel Accelerator goes live. It has three metrics, labeled definitions, and a "why did this change" panel. Dana presents it at the sales all-hands.

By Thursday, half of the reps are still opening the legacy Pipeline Report because it is bookmarked in their browser. Finance never opens either. Sofia asks the question you knew was coming: *"Is anyone using it?"*

Adoption is not a vanity metric for a data product; it is the *only* evidence that the definitions you fought for are now the definitions people decide with. If usage stays flat, you built a report.

**This week:** measure adoption properly (weekly active viewers, by role, versus the legacy dashboard), then use those numbers to decide where to spend your week: training, embedding the numbers in the CRM, or retiring the old report.`,readings:["go-to-market-launch","proof-of-value-performance"],debrief:`**What a good Week 4 looked like.**

- Weekly active viewers roughly doubled every two weeks after launch while the legacy report faded. You retired the legacy report on 1 July with a redirect, not a memo.
- Sales Managers adopted first, Sales Reps followed once a manager started asking about *their* stalled deals in 1:1s. Finance still had zero views: that became next week's stakeholder conversation with Tom\xe1s.
- You reported adoption as **distinct viewers per role**, not raw views. Raw views were inflated by two RevOps analysts refreshing all day.

Next week the number moves in the wrong direction and everyone has a theory.`},{number:5,slug:"proof",title:"Conversion fell and everyone has a theory",week:"Week 5 · Proof",tagline:"Walk down the metric tree. Don't argue at the top of it.",brief:`**Tuesday, 08:15, exec staff.** Sofia puts the chart up. Conversion rate for deals created in **May and June** is well below the first four months of the year.

- **Dana:** "It's the price increase in May. I said so."
- **Lena:** "It's the new outbound lead-scoring vendor. The leads are garbage."
- **Raj:** "Are we sure it isn't the stale-stage problem from week 3?"
- **Tom\xe1s:** "Is this deal count or deal value?"

Four hypotheses, one number. This is what the Metric Dependency Tree is for. A drop at the top has to show up somewhere at the bottom: a segment, a stage, a rep, a source. Root-cause analysis is walking *down* the tree with a query at each level until one branch explains most of the move.

**This week:** confirm the drop is real (and not an artifact of open deals), then slice it by region \xd7 source, by stage velocity, and by rep. Bring the exec team a cause, a size, and a fix, not a theory.`,readings:["proof-of-value-performance","funnel-conversion-analysis","oee-manufacturing-case-study"],debrief:`**What a good Week 5 looked like.**

- The drop was real for *resolved* deals; once you excluded open deals created in July and August, the story was clean.
- It was concentrated in **AMER \xd7 Outbound**. Every other segment was flat. That killed the price-increase theory in one query (a price increase hits every region and source).
- Time in *negotiation* for that segment roughly doubled: the leads were reaching negotiation but not closing. Lena's vendor theory held; Raj's stale-stage theory explained only a handful of deals.
- You sized it (a specific number of deals and dollars) and proposed the fix (revert the vendor's scoring model for AMER, re-qualify the open May–June outbound deals). Sofia approved it in the meeting.

One week left, and the CFO wants a revenue number for the board.`},{number:6,slug:"beyond",title:"One number, three meanings",week:"Week 6 · Beyond",tagline:"Reconcile revenue, then make the definitions machine-readable.",brief:`**Thursday, 16:00.** Tom\xe1s sends a one-line email: *"Board deck says revenue 7.77M. Sales deck says 6.84M. Which one do I put on the slide?"*

Neither is wrong. One is the sum of **transactions** (cash movements, including renewals and upsells). The other is the sum of **closed_won deal amounts** (bookings). They diverge exactly where a business is healthy: existing customers paying more. But if nobody writes that down, an AI agent someone is piloting for "revenue questions" will pick whichever definition it finds first.

That is the ontology gap, and it is your last deliverable: a reconciliation that explains every dollar of the difference, a running revenue total by month, and one capstone query that computes the whole metric tree from a single set of definitions.

**This week:** reconcile, then write it down in a form both people and agents can consume. You are no longer fixing a dashboard. You are defining what Meridian means when it says a word.`,readings:["ontology-as-infrastructure","agent-observability-gap","lean-ai-cost-economics"],debrief:`**What a good Week 6 looked like.**

- Revenue got two names with owners: \`bookings_value\` (Sales, from deals) and \`revenue_generated\` (Finance, from transactions). The board slide shows both and the bridge between them: renewals, upsells, and the duplicate rows you removed in week 3.
- The running-total query became the first "gold" metric with a written definition, a test, and an SLO. Every future metric follows the same template.
- You handed the definitions to the agent pilot as a semantic contract, and asked for an evaluation set before it answers a single executive. Observability tells you it ran; evaluation tells you it was right.

**Six weeks in.** Meridian moved from *Reactive* to *Purpose-driven* on one domain. That is what a proof of value is: not every metric, one metric tree that people decide with.`}];e.s(["CAST",0,[{name:"Sofia Marin",role:"CEO, your sponsor",agenda:"Wants proof within six weeks that a Data PM is worth the headcount. Cares about one thing: decisions getting made faster, on numbers people trust."},{name:"Dana Whitfield",role:"VP Sales",agenda:"Has stopped opening the legacy Pipeline Report. Wants a 'Sales Funnel Accelerator' that tells her where deals are stuck and why. Distrusts any conversion rate above 50%."},{name:"Raj Patel",role:"Analytics Engineer",agenda:"Owns the three pipelines (crm_deals_ingest, transactions_ingest, gold_sales_metrics). Every run is green. Tired of being told the numbers are wrong without anyone saying which number."},{name:"Lena Fischer",role:"RevOps Lead",agenda:"Owns CRM hygiene. Knows reps update deal stages late and sometimes not at all. Brought in a new outbound lead-scoring vendor in May."},{name:"Tomás Reyes",role:"CFO",agenda:"Needs a revenue number for the board that matches what finance recognizes, not what sales booked. Asks what 'revenue' means every time someone says it."}],"chapters",0,t,"getChapter",0,function(e){return t.find(t=>t.number===e)}])},74581,e=>{"use strict";var t=e.i(71645);function a(e=new Date){let t=e.getFullYear(),o=String(e.getMonth()+1).padStart(2,"0"),n=String(e.getDate()).padStart(2,"0");return`${t}-${o}-${n}`}let o="dpm-lab:progress:v1";function n(){return{version:1,user_id:"local",exercises:{},attempts:[],srs:{},lessons:{},streak:{current:0,lastActiveDate:null}}}function s(){try{let e=window.localStorage.getItem(o);if(!e)return n();let t=JSON.parse(e);if(t?.version!==1)return n();return{...n(),...t}}catch{return n()}}function r(e){let t,o=a(),{current:n,lastActiveDate:s}=e.streak;if(s===o)return e;let r=s===((t=new Date).setDate(t.getDate()-1),a(t))?n+1:1;return{...e,streak:{current:r,lastActiveDate:o}}}let i=n(),d=null,l=new Set;function u(){return null===d&&(d=s()),d}function c(){return i}function h(){for(let e of l)e()}function m(e){l.add(e);let t=e=>{e.key===o&&(d=s(),h())};return window.addEventListener("storage",t),()=>{l.delete(e),window.removeEventListener("storage",t)}}e.s(["dueCards",0,function(e,t){return t.filter(t=>(function(e,t=new Date){return!e||e.due<=a(t)})(e.srs[t.id]))},"useProgress",0,function(){let e=(0,t.useSyncExternalStore)(m,u,c),s=(0,t.useCallback)(e=>{var t=e(u());d=t;try{window.localStorage.setItem(o,JSON.stringify(t))}catch{}h()},[]),l=(0,t.useCallback)((e,t,a,o)=>s(n=>{let s,i,d,l,u;return s=new Date().toISOString(),i=n.exercises[e]??{kind:t,status:"not_started",attempts:0},d=o||"solved"===i.status?"solved":"attempted",l={...n.exercises,[e]:{...i,kind:t,status:d,attempts:i.attempts+1,lastAttemptAt:s,solvedAt:o&&!i.solvedAt?s:i.solvedAt}},u=[{id:`${e}:${s}`,slug:e,kind:t,code:a,passed:o,at:s},...n.attempts].slice(0,200),r({...n,exercises:l,attempts:u})}),[s]),p=(0,t.useCallback)((e,t)=>s(o=>{let n;return n=o.srs[e]??function(e=new Date){return{ease:2.5,interval:0,repetitions:0,due:a(e)}}(),r({...o,srs:{...o.srs,[e]:function(e,t,o=new Date){let{ease:n,interval:s,repetitions:r}=e;return t<3?(r=0,s=1):(s=0===r?1:1===r?6:Math.round(s*n),r+=1),(n+=.1-(5-t)*(.08+(5-t)*.02))<1.3&&(n=1.3),{ease:Math.round(100*n)/100,interval:s,repetitions:r,due:function(e,t){let[o,n,s]=e.split("-").map(Number),r=new Date(o,n-1,s);return r.setDate(r.getDate()+t),a(r)}(a(o),s)}}(n,t)}})}),[s]);return{data:e,hydrated:e!==i,recordAttempt:l,recordReview:p,recordLessonComplete:(0,t.useCallback)((e,t)=>s(a=>{let o,n;return o=a.lessons[e],n=Math.max(o?.bestAccuracy??0,t),r({...a,lessons:{...a.lessons,[e]:{completedAt:new Date().toISOString(),completions:(o?.completions??0)+1,bestAccuracy:n,perfect:n>=1}}})}),[s]),reset:(0,t.useCallback)(()=>s(()=>n()),[s])}}],74581)}]);